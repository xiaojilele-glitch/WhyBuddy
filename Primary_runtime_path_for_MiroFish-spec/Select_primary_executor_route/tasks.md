根据您提供的模块信息，以下是为 **Select primary executor route** 模块生成的任务清单（Markdown 格式）。该清单旨在指导开发或执行过程，确保核心逻辑的实现和数据的完整性。

---

### 任务清单：Select primary executor route (选择主要执行器路线)

#### 1. 上下文初始化与验证
- [ ] **解析输入上下文**：从父模块接收目标仓库（`MiroFish`）及备选路线集（route-set）。
- [ ] **规范化仓库标识**：确认识别并统一 `https://github.com/666ghj/MiroFish` 的内部标识符。
- [ ] **验证路线集完整性**：检查提供的路线集是否包含可用的执行器路径。

#### 2. 路线评估与决策
- [ ] **筛选执行器路径**：从路线集中过滤出符合“executor-backed role agent path”要求的备选方案。
- [ ] **确定主执行路线**：基于预设算法或优先级逻辑，从符合条件的路径中选定唯一的 **Primary Route**。
- [ ] **冲突检查**：确保选定的路线与当前的执行选项（Execution Options）无冲突。

#### 3. 核心元数据提取与记录
- [ ] **提取关键字段**：从选定的路线中获取以下核心数据：
    - `id`: 唯一标识符
    - `kind`: 路由类型（如 Agent-based, Direct 等）
    - `title`: 路线标题
    - `summary`: 路线简述
- [ ] **建立可追溯性快照**：将上述元数据保存至模块状态，以便后续进行 live callback 证据留存和审计。

#### 4. 下馈数据准备（Downstream Prep）
- [ ] **规范化任务 Prompt**：结合选定路线，格式化下游 Agent 需要的执行指令。
- [ ] **封装执行配置**：整合选定的路线元数据、仓库信息和执行选项。
- [ ] **状态固化**：锁定当前选择，防止下游组件运行时发生状态漂移。

#### 5. 质量保证与日志
- [ ] **输出审计日志**：记录选择主路线的决策原因及最终选定的 `id`。
- [ ] **异常处理**：若无匹配的执行器路线，触发“无可用路径”警报并中断后续任务。

---

**备注**：此模块是驱动 `MiroFish` 自动化执行的关键决策点，必须确保 `id` 和 `kind` 的准确性，否则会导致下游组件调用错误的 Agent 角色。