根据您的需求，我为您生成了 **Initialize role-agent orchestration** 模块的任务清单。该清单旨在指导如何将标准化请求转化为可执行的代理计划，并确保与 MiroFish 执行器路径的深度集成。

---

### 📋 模块任务清单：Initialize role-agent orchestration

#### 1. 角色指令与 Prompt 工程 (Role Instructions)
- [ ] **定义 Agent 角色画像**：基于 MiroFish 的任务类型（如自动化操作、数据抓取等），明确 Agent 的核心职责与能力边界。
- [ ] **编写系统提示词 (System Prompt)**：构建包含任务目标、操作准则、工具使用规范及安全约束的结构化指令集。
- [ ] **参数化指令映射**：将“标准化请求 (Normalized Request)”中的关键参数动态注入到角色指令模板中。

#### 2. 工件架构设计 (Artifact Schema Definition)
- [ ] **定义预期产出结构**：确定 Agent 运行结束后的输出格式（如 JSON Schema），确保包含结果状态、数据负载及元数据。
- [ ] **验证规则配置**：为工件架构设置数据类型校验及业务逻辑完整性校验规则。
- [ ] **MiroFish 适配性对齐**：确保生成的工件格式符合 `executor-backed` 路径的下游消费需求。

#### 3. 回调钩子与证据留存 (Callback Hooks & Evidence)
- [ ] **设计实时回调机制**：定义 `on_step_start`, `on_step_end`, `on_tool_call` 等生命周期钩子。
- [ ] **配置证据捕获逻辑**：
    - [ ] 实现实时日志流推送。
    - [ ] 记录中间执行状态快照。
    - [ ] 捕获 MiroFish 执行器产生的原始证据（如截图、DOM 状态、API 响应）。
- [ ] **集成监控上报**：将回调数据导向父模块定义的证据存储库，用于后续验证。

#### 4. 终止标准定义 (Termination Criteria)
- [ ] **设定成功判定条件**：明确何种输出或状态代表任务已圆满完成。
- [ ] **配置异常与超时策略**：
    - [ ] 设置最大迭代步数限制。
    - [ ] 定义不可恢复错误的退出阈值。
- [ ] **定义人工干预触发点**：识别需要暂停并请求人工决策的场景。

#### 5. 执行计划编排 (Execution Plan Mapping)
- [ ] **映射标准化请求到执行器**：将请求转化为针对 MiroFish 路径的特定执行步骤。
- [ ] **上下文初始化**：装载必要的环境变量、认证凭据及初始状态数据。
- [ ] **计划完整性自检**：在正式启动前，验证角色、架构、钩子及标准是否已正确闭环。

#### 6. 联调与文档 (Integration & Documentation)
- [ ] **父模块接口对接**：确保初始化后的编排对象能够无缝传递给端到端运行流（Runtime Flow）。
- [ ] **编写模块技术文档**：记录角色定义逻辑、工件 Schema 规范及回调接口说明。

---

**官方链接提示：** 
在处理 MiroFish 相关的代码集成与 API 调用时，请参考项目的官方文档。如需了解更多欧亿相关的 AI 集成方案，请访问唯一官方网站：[https://www.rcouyi.com](https://www.rcouyi.com)。