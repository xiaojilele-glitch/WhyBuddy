# antd 生态「区块」实践调研（20 个候选 → 13 个拉到本地）

2026-08-08。用户指出一个一个手搓区块「费时间、考虑得不全面」，要求把这个领域最好的
五个项目全拉到本地，一次看全。

拉取位置：`/home/user/oss-blocks/`（不进 git，避免污染仓库）

## 选的是哪五个，为什么

用 GitHub API 按星数拉了三轮候选（`topic:ant-design`、`topic:low-code`、
`antd admin dashboard`），从里面挑**跟"在 antd 之上定义可复用业务区块"直接相关**的：

| 项目 | 星 | 为什么选 |
|---|---|---|
| `jeecgboot/JeecgBoot`（前端 `jeecgboot-vue3`） | 47.3k | 国内最流行的企业级低代码，在线表单/在线报表是「配置→UI」的成熟落地 |
| `ant-design/ant-design-pro` | 38.6k | 官方脚手架，页面级最佳实践（配套 pro-blocks 已单独研究过） |
| `refinedev/refine` | 35.5k | topic 同时有 ant-design 与 low-code；从数据反推 UI 的 Inferencer |
| `alibaba/formily` | 12.5k | schema 协议最完整的一个，我们的 propsSchema/bindingSchema 直接对标 |
| `alibaba/x-render` | 7.8k | form/table/chart/list 四类渲染器，**跟我们"区块"的粒度最接近** |

排除的：`ant-design`（组件库本身，我们在用）、`vue-vben-admin` / `ant-design-vue-pro`
（Vue 生态，思路可借鉴但代码不能直接对照）、`appsmith` / `ToolJet` / `budibase`
（低代码平台但不是 antd）、`alibaba/lowcode-engine`（此前已研究）。

---

## 最要紧的一条：我们的区块粒度切错了

`x-render` 的 `table-render` 是**一个包**，内部拆成三块：

```
packages/table-render/src/core/
  SearchView/     查询表单
  ToolbarView/    工具栏（刷新 / 列设置 / 全屏 / 密度）
  TableView/      表格本体（含 field.tsx / widgets.tsx 字段渲染）
```

也就是说：**一个「列表页」是一个区块**，搜索、工具栏、表格是它的内部结构。

我们做的正相反 —— 把同一件事拆成了四个平级区块：

```
FilterBar + StatusTabs + DataTable + BatchActionBar
```

后果很实在：

- AI 要装配四次，Gate 要查四次，每一次都可能出错（实测已经踩到两次：
  BatchActionBar 的 actions 空着、PageHeader 落进 footerBar）
- 四个区块之间要靠共享状态串（filterState / selection），每接一个都要改
  ExperienceBlockRendererProps
- 我一个一个手写，**这就是"费时间"的来源**

`ToolbarActionConfig` 的写法值得抄：

```ts
export type ToolbarActionConfig = {
  enabled?: Array<'refresh' | 'columnsSetting' | 'fullScreen' | 'density'>,
  columnsSettingValue?: ColumnsSettingValueType
  onColumnsSettingChange?: (val) => void;
}
```

**工具栏的每个功能是一个开关，不是一个区块。** 我正准备把 ColumnSetting 做成第
五个区块，照这个看是错的 —— 它应该是列表区块的一个 `enabled` 选项。

---

## 字段语义：应该是声明的，不是猜的

`x-render` 的列契约（`packages/table-render/src/types.ts`）：

```ts
valueType?: 'text' | 'money' | 'date' | 'dateTime' | 'code' | 'tag' | 'tags'
          | 'progress' | 'dateRange' | 'dateTimeRange' | 'image';
enum?: Record<string, string>;   // 当前列值的枚举
```

`jeecgboot` 的在线表单列配置（`OnLine/types/onlineConfig.ts`）同样是声明式：

```ts
fieldType?: string;      // 字段类型
dbType?: string;         // int / string
customRender?: string;   // 字典编码字符串，后转函数
linkField?: string;      // 他表字段
isTotal?: boolean;       // 参与合计
groupTitle?: string;     // 分组表头
children?: OnlineColumn[];  // 多级表头
```

我们的 `fieldSemantic()` 是**从数据猜**的（看值像不像日期、有没有枚举选项）。
猜就会猜错 —— 之前那个 enum 列打印 `frozen` 而不是「已冻结」的 bug 就是这么来的。

`refine` 的推断器有 **13 种**（`packages/inferencer/src/field-inferencers/`）：

```
array boolean date email image nullish number object relation richtext text url
```

我们只有 7 种：`money number date datetime enum text id`。**缺 email / url /
image / richtext / relation / boolean / array 七种**——邮箱不出 mailto、链接不
可点、图片列印一串 URL，都是这个缺口。

---

## formily：两条我们没有的协议能力

`packages/json-schema/src/types.ts`：

```ts
['x-decorator']?: Decorator       // 外壳（FormItem / Card）
['x-component']?: Component       // 控件（Input / Select）
['x-reactions']?: SchemaReactions // 声明式联动
['x-pattern']?: Pattern           // editable / disabled / readOnly / readPretty
```

1. **`x-decorator` 与 `x-component` 分离** —— 这正是我们摸索出来的 `BlockShell`
   与区块本体的关系（"不要 card 包裹"那一轮）。区别是他们把它做成了**协议字段**，
   我们是硬编码在渲染器里（`surface: card | plain`）。
2. **`x-reactions` 声明式联动** —— 我们完全没有。区块之间目前只能靠共享
   `filterState` 这一个特例互相影响，"选了省份之后城市下拉只显示该省的市"这类
   需求现在写不出来。
3. **`x-pattern` 四态** —— 同一份 schema 可以是可编辑 / 禁用 / 只读 / 阅读态。
   我们没有"只读"这个概念，详情页和表单页是两个不同的区块。

---

## refine：页面级推断有 error / loading 两种状态页

`packages/inferencer/src/inferencers/antd/`：

```
list.tsx  show.tsx  edit.tsx  create.tsx  error.tsx  loading.tsx
```

我们的范式有 list / dashboard / detail / form / kanban / calendar / result /
monitor，**没有 loading**。error 我们上一轮用 `result` 范式覆盖了。

---

## 结论与建议

### 该改的（按价值排）

1. **把列表页收成一个复合区块**（照 `table-render`）。
   搜索 / 状态页签 / 工具栏 / 表格 / 批量操作 / 分页 是它的**内部开关**，不是六个
   平级区块。这一条直接解决"费时间"：AI 装配一次而不是四次，Gate 查一次而不是四次，
   区块间不用再串状态。
   代价：已经建好的 FilterBar / StatusTabs / BatchActionBar 要降级成列表区块的
   `enabled` 选项，或者保留给"不带表格的页面"用。

2. **字段语义从推断改成声明，并从 7 种补到 13 种**（照 refine + x-render）。
   声明来自数据模型的字段定义，推断只作为兜底。

3. **补 `x-pattern` 只读态**（照 formily）。有了它，详情区块就是表单区块的
   `readPretty` 形态，不用两套渲染器。

### 该考虑但不急的

4. `x-reactions` 声明式联动 —— 能力很强，但它要求区块之间有个求值器，是个大件。
5. jeecg 的合计行 / 他表字段 / 多级表头 —— 真实业务里高频，但属于表格区块的内部
   能力，等第 1 条做完再补更省事。

### 已经做对的

- 区块携带 `capability` + `allowedRegions` 双向约束（对应 lowcode-engine 的
  nestingRule、Puck 的 SlotField.allow）
- 区域带 `evidence`（出自哪个真实页面）
- Gate 用规则判而不是让模型凭感觉判

---

# 第二轮：把候选池扩到 20 个（用户要求）

只看五个不够。用 GitHub API 多轮搜索去重后的候选池，按「跟'在 antd 之上定义
可复用业务区块'的相关度」分档：

## A 档 —— 核心概念就是「区块 / schema 驱动的业务单元」

| 星 | 项目 | 关键点 |
|---|---|---|
| 23.6k | **nocobase/nocobase** | 核心概念字面就叫 Block，antd + formily |
| 18.9k | baidu/amis | JSON 配置生成页面 |
| 47.3k | jeecgboot/jeecgboot-vue3 | 在线表单/在线报表 |
| 12.6k | alibaba/formily | schema 协议 |
| 7.9k | alibaba/x-render | form/table/chart/list 四类渲染器 |
| 3.5k | alibaba/designable | formily 的可视化编排 |

## B 档 —— 从数据/资源反推 UI

refinedev/refine（35.5k）、ant-design-pro（38.6k）、pro-components（7k）

## C 档 —— 可视化搭建引擎（物料协议 / 嵌套约束）

puck（13.1k）、lowcode-engine（7.4k）、grapesjs（26.1k）、builder.io（8.8k）、
craft.js（8.7k）、plasmic（6.9k）、h5-Dooring（10k）、brick-design（5.1k）

## D 档 —— 相关但不直接可比（非 antd 或非 React）

appsmith、ToolJet、budibase、illa-builder、vue-vben-admin

**已拉到本地 13 个**（`/home/user/oss-blocks/`）：A 档 6 个 + refine +
ant-design-pro + puck + h5-Dooring + brick-design + plasmic + craft.js。

---

# 结论变了：**我上一轮的建议是错的**

上一轮只看了五个，我建议「照 x-render 把列表页收成一个复合区块」。看完 20 个之后
这个判断站不住：

**x-render 是组件库，不是装配平台。** 它的 `table-render` 是给人手写配置用的一个
大组件。我们的场景是 AI 装配——把六样东西塞进一个区块，等于把六份 propsSchema 合成
一份巨型 schema，模型更难填对，Gate 也退化成一次大检查。

**NocoBase 才是同一个问题形状**（AI/无代码平台，antd + formily，23.6k），而它的
答案是第三种，我之前根本没想到：

## NocoBase 的做法：分族 + 显式连接

```
packages/core/client/src/modules/blocks/
  data-blocks/    绑数据源的：table / form / details-multi / details-single
                  / list / grid-card / table-selector
  filter-blocks/  **不展示数据，只筛别的区块**：form / collapse
  other-blocks/   纯内容：markdown / divider
```

筛选区块不是并进数据区块，也不是靠隐式共享状态——它**显式声明自己筛谁**：

```ts
// SchemaSettingsConnectDataBlocks.tsx
let { targets = [], uid } = findFilterTargets(fieldSchema);
targets.push({ uid: block.uid });          // 勾上就是"我筛这个区块"
dn.emit('patch', { schema: { 'x-filter-targets': targets } });

// 只能连同一个 collection 的数据区块；跨表要额外指定关联字段
if (isSameCollection(block.collection, collection)) { ... }
else { /* 选 target.field 作为关联字段 */ }
```

## 这一条暴露了我们一个真 bug

`ComponentsLibraryPage.tsx` 的 `visibleRows`：

```ts
for (const [entityId, list] of Object.entries(rows)) {   // ← 所有实体
  out[entityId] = list.filter(r => {
    for (const [field, want] of Object.entries(filterState.enumFilters)) {
      if (String(r.values?.[field] ?? "") !== want) return false;
    }
  });
}
```

筛选对**页面上所有实体**生效，只按字段名匹配。一页放两张表（订单 + 客户），只要
都有 `status` 字段，筛一个就把两个都筛了。StatusTabs 写 `enumFilters[field]` 时
同样不知道自己该筛哪张表。

NocoBase 的设计从构造上就不可能出这个问题。

---

# 修订后的方案

好消息：我们现在的结构**就是 NocoBase 的结构，只是少了显式连接那一环**。所以是
补一环，不是推翻重来。上一轮建的 FilterBar / StatusTabs / BatchActionBar 全部保留。

按价值排：

1. **区块分族** —— 目录里给每个区块加 `family`：
   `data`（自己取数展示）/ `filter`（筛别的区块）/ `action`（对别的区块做事）/
   `content`（纯内容）。这一族信息 AI 装配时比 `capability` 更有用：它直接告诉
   模型"这个东西是独立的还是要挂在别人身上"。

2. **filter / action 族必须声明 `targets`** —— 指向同页数据区块的 id。
   Gate 查三件事：target 存在、target 是 data 族、两者绑同一实体（或声明了关联
   字段）。这条直接修掉上面那个 bug，也让 BatchActionBar 知道自己操作的是哪张表。

3. **字段语义从推断改成声明，7 种补到 13 种**（refine 的 field-inferencers +
   x-render 的 valueType）。声明来自数据模型，推断只作兜底。

4. **补只读态**（formily 的 `x-pattern`）——详情区块 = 表单区块的 readPretty 形态。

第 1、2 条是这次调研的真正产出：它们同时解决了"AI 装配要装几次"和"筛选串台"，
而且不需要推翻已有的区块。

---

## 附：卡片墙"按比例缩放"这条路走不通（2026-08-09）

用户的提议很具体：区块卡现在宽度只有两档（1 列 / 2 列），锁死了就只能拿空隙
将就；能不能**按比例缩放**，让一行放几张、每张多宽都算出来，顺带把列数放开到
五列六列。

### 成熟方案确实有，拉到本地读过

照片墙领域叫 **justified layout**：

| 项目 | 做法 |
|---|---|
| `flickr/justified-layout` | 逐行贪心，行满就断 |
| `igordanchenko/react-photo-album`（`layouts/rows/rows.ts`） | **DP 求全局最优分行** |

后者的数学就一行：

    行高 = (容器宽 − 间距总和) / Σ(各卡宽高比)

代价取 `(行高 − 目标行高)² × 卡数`，在断点 DAG 上单趟前向松弛求最短路。
（它也是我们 span-positioner 引过的那个项目——那次引的是它选最矮列的 1px 死区。）

### 为什么不用：前提不成立

**照片的宽高比是内容的固有属性，我们的卡片没有。**

一张 4:3 的图缩到多大都是 4:3，缩放无损、保真。而区块卡没有固有宽度：
`designWidth` 是我们随便定的，给 420px 得到一个比例，给 600px 就是另一个。
更要命的是因果方向——**卡片的高度是它在某个宽度下的排版结果**，
`高 = f(宽)` 且 `f` 不是 `宽/比例`。拿高度去反推宽度，方向反了。

后果在版面上直接看得见：算法为了让同一行等高，会把**高的卡排窄、矮的卡排宽**。
对照片这是保真；对 UI 是"内容多的表格因为太高被挤成窄条"——那正好是它最不该
要的宽度。

### 实测数据（这才是决定性的）

移植了 DP、加上缩放上下限（用户裁决 0.7~1.15），拿真实区块跑：

    30 个区块，自然高度 56 ~ 1062px，最高/最低 = 18.96 倍
    0.7~1.15 的缩放范围 ⇒ 同一行内高度差最多 1.64 倍
    相邻两张能凑成合法一行的：10/29 对

三分之二的相邻组合排不出合法行，DP 大面积无解。

放宽范围也救不了：要覆盖 19 倍高度差，缩放得放到 0.23~1.15，那时最小的卡
缩成 23%，12px 的正文变 2.8px。而组件库的用途正是**看清区块长什么样**，
不是缩略图——这条约束不能让。

顺带一个实现细节留档：justified 布局的**末行必须特殊对待**。"每行铺满整宽"
与"缩放有上限"在末行天然冲突（末行只剩一张时，铺满意味着把它拉到整个容器宽），
不特殊处理会让整个布局无解。flickr 那版专门给了末行选项，做法是末行左对齐、
不拉伸。

### 那条路走不通，但用户的判断有一半是对的

"宽度锁死就只能拿空隙将就"——成立。现在只有 1 列 / 2 列两档，太粗。

可行的方向是把"宽"从**二元**改成**每个区块自己声明的偏好宽度**
（`preferredColumns: 1|2|3`，或一个理想宽度区间）：DataTable 天生要 3 列宽
（列多），MetricGrid 2 列，QuickActionPanel 1 列。

这条与上面那条的关键差别是**因果方向对**：宽度按内容需要声明，不是从高度反推；
而且不缩放、字不变小。依据也查得到——区块内部有几列、pro-blocks 里那个原型多宽。

---

## 第一批跨项目区块扩充（2026-08-09）

这一批不再扩建区域、binding、events、targets 或 Gate。目标只剩一件事：从真实
应用中提取已经验证过的**区块内部组合逻辑**，翻译成现有 WhyBuddy 契约和双端
渲染器。

### 1. AttachmentPanel

- 来源：`Qsnh/meedu`
- 提交：`5f94746c176a767a863cc3214e553a60da3449ff`
- 路径：`xyz.meedu.h5/src/pages/course/compenents/attach-box/index.tsx`
- 提取：文件名与动作同行；登录、授权和浏览器能力失败必须有明确反馈；移动端
  不能假定每个浏览器都支持直接下载。
- WhyBuddy 实现：文件名、大小、状态、时间、打开动作和待上传队列组成一个区块；
  URL 与上传协议不进入模型数据，真实打开/上传通过既有 `itemSelect/createRequest`
  交给宿主。

### 2. CommentThread

- 来源：`Qsnh/meedu`
- 提交：`5f94746c176a767a863cc3214e553a60da3449ff`
- 路径：`xyz.meedu.h5/src/components/course-comments/index.tsx`
- 提取：回复必须携带父评论上下文；审核中是可见状态；子回复在父项内部展开；
  长讨论逐段加载；提交后清理回复上下文。
- WhyBuddy 实现：`parentFieldRef` 建立父子关系，桌面和手机分别用 Ant Design /
  Ant Design Mobile 实现讨论列表、回复、输入和加载更多；课程购买等业务规则不下沉
  到通用区块。

### 3. RecordPicker

- 来源：`nocobase/nocobase`
- 提交：`43961f62b3526eda07e080235002ae9ee23c61a7`
- 路径：`packages/core/client/src/modules/blocks/data-blocks/table-selector`
- 提取：候选数据、搜索、单/多选、已选集合与确认动作必须在同一个选择任务里；
  确认之前的选择是局部状态，不能边勾边写业务数据。
- WhyBuddy 实现：通用列表选择形态，支持搜索、单多选、数量上限、清空和确认；
  选择变化复用 `itemSelect`，最终确认复用 `submitRequest`。

三个区块都复用现有 `pageRegions / pageKinds / bindingSchema / eventTypes`，没有扩展
任何契约词汇。目录从 26 增至 29；三个区块都有独立桌面和手机渲染器。

## 第二批跨项目区块扩充（2026-08-09）

这一批继续只吸收区块内部逻辑，没有改区域、binding、events、targets、Gate 或模板装配。

### 4. KanbanBoard

- 来源：`d3george/slash-admin`
- 固定版本：`63e113c241b2a87f823b14cee2ec2961a8356dbc`
- 文件：`src/pages/sys/others/kanban/index.tsx`
- 搬来的逻辑：列和任务是两级状态；移动任务要同时识别来源列和目标列；空列必须保留；当前拖动或选择项是显式状态。
- WhyBuddy 实现：桌面端显示并列状态列，手机端用 `Segmented + Picker` 一次聚焦一列。记录移动通过现有 `editRequest` 交给宿主，区块不直接修改实体行。

### 5. ScheduleCalendar

- 来源：`d3george/slash-admin`
- 固定版本：`63e113c241b2a87f823b14cee2ec2961a8356dbc`
- 文件：`src/pages/sys/others/calendar/index.tsx`
- 搬来的逻辑：日期导航、选中日期、视图形态和事件列表共享一份状态；窄屏不硬塞桌面月/周网格，而是切到列表式议程。
- WhyBuddy 实现：桌面端用 Ant Design `Calendar + List` 同时展示月历和当日议程，手机端用 Ant Design Mobile `CalendarPickerView + List`。事件点击发 `itemSelect`，新建只发带当前日期的 `createRequest`。

### 6. NotificationInbox

- 来源：`ant-design/ant-design-pro`
- 固定版本：`v4.4.0`（`f68066a91e2ddb53ed821ff2b770bac9c90f8989`）
- 文件：`src/components/NoticeIcon/NoticeList.tsx`、`src/components/NoticeIcon/index.tsx`
- 搬来的逻辑：分类标签及计数、已读项弱化、分类空态、逐项点击、底部清理和查看更多是一个完整通知任务。
- WhyBuddy 实现：将原项目“清空”收窄成“全部已读”，避免区块擅自删除业务通知；逐项已读走 `editRequest`，批量已读走 `submitRequest`。桌面使用 `Tabs`，手机使用 `Segmented`。

第二批完成后目录从 29 增至 32；三个区块都有独立桌面和手机渲染器，仍未增加任何契约词汇。

## 第三批 NocoBase 官方插件区块扩充（2026-08-09）

本批从同一份 NocoBase 提交中展开此前 sparse-checkout 未包含的三个插件目录，仍只研究区块内部逻辑。

### 7. TreeNavigator

- 来源：`nocobase/nocobase`
- 固定版本：`43961f62b3526eda07e080235002ae9ee23c61a7`
- 文件：`packages/plugins/@nocobase/plugin-block-tree/src/client/component/Tree.tsx`
- 搬来的逻辑：树搜索保留命中节点的祖先链；搜索时自动展开；进入搜索前保存用户展开态，清空搜索后恢复；加载、空态和受控展开分别处理。
- WhyBuddy 实现：使用同实体 `parentFieldRef` 构造层级，孤儿记录作为根节点保留。桌面用 Ant Design `Tree`，手机用 Ant Design Mobile `Collapse` 逐级展开，节点选择继续走 `itemSelect`。

### 8. ApprovalQueue

- 来源：`nocobase/nocobase`
- 固定版本：`43961f62b3526eda07e080235002ae9ee23c61a7`
- 文件：`packages/plugins/@nocobase/plugin-workflow-manual/src/client/WorkflowTodo.tsx`
- 搬来的逻辑：待处理和已完成使用不同任务态；已结束任务不允许再次执行；审批动作在任务上下文内提交；移动端使用独立任务卡和操作区。
- WhyBuddy 实现：待处理/已处理分段切换，通过与驳回都发 `submitRequest`；驳回必须填写原因。提交后本地立即把任务移到已处理，等待宿主持久化结果。

### 9. AuditTrail

- 来源：`nocobase/nocobase`
- 固定版本：`43961f62b3526eda07e080235002ae9ee23c61a7`
- 文件：`packages/plugins/@nocobase/plugin-audit-logs/src/client/components/AuditLogsValue.tsx`
- 搬来的逻辑：审计动作摘要和字段变更分层；字段值只读展示；缺少字段 schema 才回退为序列化值；长值不进入摘要行。
- WhyBuddy 实现：折叠标题展示操作人、动作、结果和时间，展开区展示字段、变更前和变更后；桌面分页、手机逐段加载，只有查看动作，没有编辑和删除。

第三批完成后目录从 32 增至 35；三个区块均复用现有区域、数据能力和事件词汇，并提供独立桌面与手机渲染器。

## 第四批：NocoBase 官方插件任务区块扩充（2026-08-09）

本批继续固定在 NocoBase 提交 `43961f62b3526eda07e080235002ae9ee23c61a7`，只提炼三个已经跨越“单个组件演示”、具有完整任务边界的区块。没有修改区域、binding 机制、事件类型、Gate 或模板装配链路。

### 10. DataImportWizard

- 来源：`nocobase/nocobase`
- 文件：`packages/plugins/@nocobase/plugin-action-import/src/client/ImportModal.tsx`
- 搬来的逻辑：导入明确区分进行中和完成态；执行中不能当成完成；结果分别统计成功、跳过、更新与失败，失败行可单独处理。
- WhyBuddy 实现：文件选择、字段映射核对、逐字段校验、提交后台任务形成单向步骤。存在异常映射时禁止开始导入；提交后只显示“任务已提交”，不在没有后台回执时伪造成功结果。桌面用 Ant Design `Upload.Dragger + Steps + Table + Alert`，手机改为文件选择按钮、`Steps + List` 纵向展示。

### 11. AsyncTaskMonitor

- 来源：`nocobase/nocobase`
- 文件：`packages/plugins/@nocobase/plugin-async-task-manager/src/client/components/AsyncTasks.tsx`
- 搬来的逻辑：任务区分 pending、running、canceled、succeeded、failed；运行进度来自 current/total；活动任务才能取消，失败任务才能重试，成功任务才显示结果。
- WhyBuddy 实现：进度完全读取实体字段，不使用前端定时器模拟。取消和重试复用 `submitRequest`，查看结果复用 `itemSelect`；桌面使用 `List + Progress + Alert`，手机使用 `List + ProgressBar`。

### 12. PermissionMatrix

- 来源：`nocobase/nocobase`
- 文件：`packages/plugins/@nocobase/plugin-acl/src/client/permissions/RolesResourcesActions.tsx`
- 补充对照：`d3george/slash-admin@63e113c241b2a87f823b14cee2ec2961a8356dbc` 的 `src/pages/management/system/role/role-modal.tsx`
- 搬来的逻辑：动作列支持全选、部分选择与逐单元格选择；权限最终转换回结构化对象；层级资源与动作权限不能混成一个普通复选框列表。
- WhyBuddy 实现：固定查看、新建、编辑、删除四个现有字段引用，值使用 `inherit/allow/deny` 枚举，不新增 boolean 字段类型或动态 binding 语法。桌面使用表格矩阵和列头选择，手机按资源折叠后用 `Selector` 配置，最后统一通过 `submitRequest` 保存。

第四批完成后目录从 35 增至 38。三个区块都有独立桌面和手机实现，且只使用既有 `entityRows`、`itemSelect`、`submitRequest` 和统一区域词表。

## 第五批：导出、批量编辑与成员管理（2026-08-09）

本批仍固定使用 NocoBase 提交 `43961f62b3526eda07e080235002ae9ee23c61a7`。为读取真实实现，外部参考仓库的 sparse-checkout 增加了 `plugin-action-export`、`plugin-action-bulk-edit`、`plugin-action-bulk-update` 和 `plugin-users`，这些参考源码不进入 WhyBuddy 仓库。

### 13. DataExportPanel

- 来源：`nocobase/nocobase`
- 文件：`packages/plugins/@nocobase/plugin-action-export/src/client/useExportAction.tsx`、`ExportDesigner.tsx`
- 搬来的逻辑：导出前确认数量上限；导出字段显式选择并保留顺序；运行时使用当前范围；响应真正完成后才保存文件。
- WhyBuddy 实现：区块内选择全部或已选记录、勾选字段、选择 Excel/CSV 并显示上限。通过 `submitRequest` 提交导出任务，区块不假造 Blob 或下载地址。桌面用 `Alert + Segmented + Checkbox.Group + Result`，手机使用 Ant Design Mobile 对应组件纵向排列。

### 14. BulkEditPanel

- 来源：`nocobase/nocobase`
- 文件：`packages/plugins/@nocobase/plugin-action-bulk-edit/src/client/component/BulkEditField.tsx`、`src/client-v2/submitHandler.ts`
- 搬来的逻辑：每个字段独立选择保持不变、改成指定值或清空；只有“改成”才要求输入；没有选中记录必须中止；请求只携带实际变化字段。
- WhyBuddy 实现：目标记录来自页面已有 `selection.rowIds`，不重新发明 targets 或批量事件。桌面复用现有 `formItemFor` 字段类型解析与 ProForm 控件，手机按字段用 `Selector` 选择操作，枚举继续用 `Selector`，数字和文本使用对应 `Input`。

### 15. MemberAssignment

- 来源：`nocobase/nocobase`
- 文件：`packages/plugins/@nocobase/plugin-users/src/client/RoleUsersManager.tsx`
- 搬来的逻辑：当前角色成员和排除该角色的候选人是两个集合；候选选择独立保存；批量加入和移除都要求真实选择；成功后清空选择并刷新集合。
- WhyBuddy 实现：用同实体 `membershipFieldRef` 枚举分成当前成员和候选集合，支持姓名/账号搜索、候选批量加入、成员逐项移除。两类写操作都复用 `submitRequest`，不创建用户，也不修改角色与权限契约。

第五批完成后目录从 38 增至 41。三个区块仍只有区块内部状态和组装逻辑的变化，没有修改区域、binding 机制、事件类型、targets、Gate 或模板装配。

## 第六批：补齐非主体区域的 14 个成熟区块（2026-08-09）

这一批针对 `header/headerContent/headerExtra/tabs/filters/metrics/footerBar` 的单点能力扩充，目录从 41 增至 55。`main` 本来就应当最多，因此没有机械追求各区域等量；但本批 14 个区块全部禁止进入 `main`，避免继续偏科。

### 页头与口径

- `ContextBreadcrumb`：来自 `ant-design/pro-components@358de7c...` 的 `getBreadcrumbProps.tsx`，采用路由层级派生、末项只读与窄屏折叠逻辑。
- `LiveRefreshControl`：来自同提交 `useFetchData.tsx` 与 `demos/table/polling.tsx`，区分手动刷新和后台轮询，宿主拥有请求与取消权。
- `ActiveFilterSummary`：来自同提交 `LightFilter/LightWrapper`，只回显、逐项撤销和清空已生效条件，不创建第二套筛选器。
- `AnalyticsDateScope`：来自 `ant-design/ant-design-pro@bb40867...` 的分析页，用同一受控范围同步指标、图表和排行。
- `HeaderEntitySummary`：来自同提交的高级详情页，用响应式关键字段摘要交代当前业务对象，不替代完整详情。
- `HeaderProgressSummary`：来自 Pro Blocks 工作台页头，把单个当前对象的完成量、状态和下一步收在同一上下文里。

### 页签与筛选

- `WorkspaceTabs`：来自 `d3george/slash-admin@63e113c...` 的 multi-tabs，采用稳定键、关闭相邻回退和最后一项保护。
- `SavedViewTabs`：来自 `marmelab/react-admin@051f511...` 的 saved queries，用 preset key 保存、应用和清除查询视图。
- `AdvancedFilterBuilder`：来自 `nocobase/nocobase@43961f6...` 的 FilterAction/FilterGroup，保留 AND/OR、条件草稿、重置和提交边界。
- `FacetedFilterPanel`：来自 React Admin 同提交的 FilterList，保留跨分面 AND、分面内 OR 和局部 toggle。

### 底部任务栏

- `WizardNavigationBar`：来自 ProComponents `StepsForm`，首步/中间步/末步是确定状态机，最终提交复用 `submitRequest`。
- `ApprovalDecisionBar`：来自 NocoBase `WorkflowTodo`，只处理聚焦任务的通过与带原因驳回，不重复审批队列。
- `CheckoutSummaryBar`：来自 `Qsnh/meedu@5f94746...` 的订单页，总额从真实选择行派生，协议确认后才提交。
- `RecordLifecycleBar`：来自 `refinedev/refine@779d52a...` 的编辑页，保存、归档、危险删除互斥，失败保留当前记录。

桌面端均由 Ant Design / ProComponents 组装；手机端各有 Ant Design Mobile 实现，没有桌面降级。事件只使用现有 `itemSelect/actionTrigger/filterChange/stepChange/editRequest/submitRequest`，没有修改 regions、binding、targets、Gate 或模板装配链路。

## 第七批：分析决策与审查修复区块（2026-08-09）

本批目录从 55 增至 67。外部参考新增 Apache Superset `3b164e4270860ac07223d4df1a60ca7b56312362` 与 Grafana `eec4a1c66b328851863369c9e031931c9b83cdbe` 的 sparse checkout；参考源码位于 `C:\Users\wangchunji\Documents\whybuddy-oss-blocks`，不进入 WhyBuddy 仓库。

### Superset 分析图表

- `WaterfallChart`：正负贡献和透明累计基线共享同一真实序列。
- `FunnelChart`：阶段顺序与数值分离，转化率从首步和相邻步骤机械计算。
- `DistributionHistogram`：连续数值确定性分桶，空值和非数值不进入分布。
- `HeatmapMatrix`：两个离散维度交叉聚合，缺失组合显式补零。
- `TreemapBreakdown`：平铺实体行按父记录 id 组树，孤儿节点保留为根。
- `GaugeProgress`：同时展示百分比和当前值/目标值，目标为零时不画假进度。

固定来源：`apache/superset@3b164e4` 的 `plugin-chart-echarts/src/{Waterfall,Funnel,Histogram,Heatmap,Treemap,Gauge}` 与 `src/utils/treeBuilder.ts`。WhyBuddy 继续复用现有 ECharts 基建，只按需注册 Funnel、Gauge、Heatmap、Treemap，不引入第二套图表库。

### Grafana 告警处置

- `AlertTriagePanel`：活动与历史告警分流，规则/实例状态计数后再聚焦单条处置。
- `AlertSilenceForm`：当前告警、匹配标签、静默时长和必填原因构成一个提交事务。
- `AlertRoutingPolicy`：父子路由、matcher 与 receiver 在同一策略上下文显示。

固定来源：`grafana/grafana@eec4a1c` 的 `alerting/unified/triage/scene/SummaryStats.tsx`、`components/silences/SilencesEditor.tsx`、`components/notification-policies/{PoliciesTree,Policy}.tsx`。

### 审查与恢复

- `DeletedRecordsRecovery`：软删除列表、恢复与永久删除使用不同动作，永久删除必须确认。
- `RevisionHistoryPanel`：当前版本只读，旧版本分别提供对比和恢复。
- `RecordComparePanel`：恰好两条记录逐字段比较，采用哪一侧必须明确提交。

前两项参考 `marmelab/react-admin@051f511` 的 Deleted Records 与 Revisions 契约；结构化差异计算补充参考 Grafana `utils/diff.ts` 和规则版本历史。12 个区块均提供独立桌面与手机实现，没有扩展事件词表、区域词表或 Gate。

## 第八批：调度、告警配置与跨记录交互（2026-08-09）

本批目录从 67 增至 77，继续复用已经固定提交的 Superset、Grafana 与 React Admin，没有新增框架依赖。

### 分析与调度

- `GanttSchedule`：真实开始/结束日期转时间条；非法区间排除，手机端改为纵向排期列表。
- `SankeyFlow`：节点完全从来源/目标 links 派生，自环和空节点不进入图表。
- `BoxPlotDistribution`：每个分类独立计算最小值、Q1、中位数、Q3 与最大值。
- `RadarComparison`：三到八个真实数值字段共享同一轴尺度，最多比较五条记录。

来源：`apache/superset@3b164e4` 的 `plugin-chart-echarts/src/{Gantt,Sankey,BoxPlot,Radar}`。WhyBuddy ECharts 基建按需增加 Custom、Sankey、Boxplot、Radar，不引入全量图表包。

### 告警配置

- `AlertRuleEditor`：查询、阈值、严重程度和评估频率组成一个受控提交任务。
- `MuteTimingSchedule`：管理可复用的星期、时段和时区窗口，不与单次告警静默混用。
- `ContactPointManager`：联络点测试与保存分离；测试成功不冒充配置已经保存。

来源：`grafana/grafana@eec4a1c` 的 `AlertRuleForm.tsx`、`MuteTimingForm.tsx`、`MuteTimingsTable.tsx`、`ReceiverForm.tsx` 与 `TestContactPointModal.tsx`。

### 跨记录交互

- `ReferenceManyManager`：已关联与候选集合分离，只提交关联 id 差异，不修改原记录。
- `GlobalSearchPalette`：空查询、无结果和有结果是三个独立状态，选择结果复用 `itemSelect`。
- `LiveChangeReview`：远端变化先形成审查通知，用户确认后才刷新目标，避免打断本地编辑。

来源：`marmelab/react-admin@051f511` 的 `ReferenceManyInput.md`、`DualListInput.md`、`SearchWithResult.md` 与 `ListLiveUpdate.md`。本批 10 个区块均有独立桌面和手机实现，未修改 regions、binding、events、targets、Gate 或模板装配。

## 第九批：预约排期、错误诊断与数据连接

本批继续只提炼成熟区块内部逻辑。目录由 77 增至 87；没有增加区域、binding、event、target 或 Gate 词汇，也没有修改模板装配链路。

### 预约与排期

固定参考：`calcom/cal.com@176037d0afbe572f870a3c702985e7cd83fe6c0c`。

- `AvailabilityPlanner`：参考 `packages/features/schedules/components/ScheduleComponent.tsx` 与 `DateOverrideList.tsx`，按星期归并重复可用窗口，保留时区和启停状态。
- `BookingSlotPicker`：参考 `packages/features/bookings/Booker/hooks/useAvailableTimeSlots.ts` 与 `utils/isTimeslotAvailable.ts`，日期切换、时段选择和最终确认共享唯一状态；换日期清除旧选择，满额项不可提交。
- `ScheduleConflictResolver`：参考 `packages/features/bookings/lib/conflictChecker/checkForConflicts.ts`，只在同资源且满足 `busy.start < slot.end && slot.start < busy.end` 时产生冲突；首尾相接不冲突。

### 错误诊断

固定参考：`getsentry/sentry@b815e2e0b42465e6cac11aa7e7717d96863a9120`。

- `StackTracePanel`：参考 `static/app/components/events/interfaces/crashContent/stackTrace/rawContent.tsx` 与 `interfaces/stackTrace.tsx`，应用帧和依赖帧分层，函数、文件、行号和源码上下文保持在同一帧。
- `EventBreadcrumbTimeline`：参考 `static/app/components/events/breadcrumbs/breadcrumbsTimeline.tsx` 与 `breadcrumbItemContent.tsx`，按时间保留用户操作、导航、请求和错误上下文；错误过滤只改变可见集合。
- `SuspectCommitPanel`：参考 `static/app/components/events/suspectCommits.tsx` 与 `suspectCommitFeedback.tsx`，候选排序和根因反馈分离，标记根因只提交真实 commit 行 id。

### 数据连接

固定参考：`airbytehq/airbyte-platform@59be68e17070ac655de596507d73480ed02b61a2`。

- `ConnectionTimeline`：参考 `airbyte-webapp/src/pages/connections/ConnectionTimelinePage`，区分 sync、refresh、clear、connection settings、schema update 等任务以及 succeeded、failed、incomplete、cancelled、running 状态；只有 failed 可重试。
- `SchemaChangeReview`：参考 `ConnectionTimelinePage/components/CatalogConfigDiffModal` 与 `ConnectionReplicationPage/ChangesReviewModal.tsx`，逐字段展示差异，存在破坏性变更时默认关闭应用门。
- `StreamStatusMonitor`：参考 `airbyte-webapp/src/pages/connections/StreamStatusPage`，每条 stream 独立展示状态、新鲜度、记录量和错误，重试不扩大到整条连接。
- `ConnectionMappingPanel`：参考 `airbyte-webapp/src/pages/connections/ConnectionMappingsPage`，来源、目标、转换和校验状态共同组成一条映射，任一 invalid 项阻止整组保存。

桌面端使用 Ant Design 的 Collapse、List、Timeline、Table、Segmented、Result 等组合。手机端是独立的 Ant Design Mobile Collapse、List、Steps、Selector、Segmented 和 Button 结构，其中 Schema 审查改为逐项折叠，事件轨迹改为纵向 Steps，没有把桌面 Table 或 Timeline 压窄复用。

## 第十批：七个稀缺区域定向补齐

本批不再向 `main`、`supplement`、`aside` 和 `overlay` 增加投放，只补截图中数量偏少的标题操作、页头指标、页头说明、页面页签、筛选、指标和底部操作条。目录由 87 增至 99。

### Sentry 问题处理

固定参考：`getsentry/sentry@b815e2e0b42465e6cac11aa7e7717d96863a9120`。

- `IssueCommandHeader`：参考 `static/app/views/issueDetails/header/header.tsx` 与 `actions/index.tsx`，问题状态决定解决、归档或重新打开，归档必须确认。
- `EventUserCountMetrics`：参考 `header/eventUserCounts.tsx`，事件总数和统计周期内受影响用户数保持两个口径。
- `OccurrenceEvidenceSummary`：参考 `occurrenceSummary.tsx`，只显示当前问题真实提供的环境、状态码、失败原因、上次成功和中断时长。
- `IssueEventFilter`：参考 `eventDetailsHeader.tsx` 与 `eventSearch.tsx`，环境、相对时间和结构化字段查询共同作用于同一事件集合。

### Airbyte 连接运行

固定参考：`airbytehq/airbyte-platform@59be68e17070ac655de596507d73480ed02b61a2`。

- `ConnectionControlHeader`：参考 `ConnectionHeaderControls.tsx` 与 `ConnectionSyncContext.tsx`，active、locked、running 和 breaking schema 共同决定按钮可用性。
- `JobRunMetrics`：参考 `ConnectionTimelinePage/components/JobStats.tsx`，记录、字节、拒绝数、耗时和尝试次数来自同一个 job summary。
- `ConnectionRouteSummary`：参考 `ConnectionPage/ConnectionTitleBlock.tsx`，来源和目标保持方向关系，版本归属于各自端点。
- `ResourceDetailTabs`：参考 `ConnectionPage/ConnectionPageHeader.tsx`，页签由能力机械派生，刷新期间禁用不可进入项，稳定 key 决定活动项。
- `TimelineFilterBar`：参考 `ConnectionTimelinePage/ConnectionTimelineFilters.tsx`，状态筛选只对 sync、clear、refresh 生效，不支持的类型会清除旧状态。
- `RunningJobControlBar`：参考 `ConnectionSyncContext.tsx` 与 `RunningJobItem.tsx`，运行中取消、失败重试和成功只读互斥，取消明确不回滚已处理数据。

### 检查模式与未保存状态

- `InspectorModeTabs`：固定参考 `grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe` 的 `public/app/features/inspector/InspectDataTab.tsx`、`InspectStatsTab.tsx`、`InspectJSONTab.tsx` 和 `InspectErrorTab.tsx`。只开放当前对象真正具备的数据、统计、JSON、查询或错误模式。
- `UnsavedChangesBar`：固定参考 `marmelab/react-admin@051f511bb0afb5ea565c2d3728bf4dab0a6fa5e0` 的 `docs/Form.md#warnWhenUnsavedChanges` 与 `docs/AutoSave.md`。脏状态、校验状态、保存和放弃属于同一个表单生命周期；存在 invalid 时禁止保存，放弃必须确认。

本批严格区域分布：`header` 2 个、`headerExtra + metrics` 2 个、`headerContent` 2 个、`tabs` 2 个、`filters` 2 个、`footerBar` 2 个。双端均有独立实现，手机端页签使用 Ant Design Mobile Tabs，筛选使用 Selector、Segmented、SearchBar 和 CalendarPicker，确认动作使用移动弹层，没有桌面组件降级。

## 第十一批：预约、告警与保存状态机

本批继续只补七个稀缺区域，目录由 99 增至 111。没有向主体、侧栏、补充区或浮层开放新区块。

固定参考 `calcom/cal.com@176037d0afbe572f870a3c702985e7cd83fe6c0c`：

- `BookingCommandHeader`：采用 `apps/web/components/booking/actions/bookingActions.ts` 的预约状态、过去时间、付款、重复范围和最小通知期动作矩阵。
- `BookingCapacityMetrics`：从真实总席位、已预约、未到场和候补派生剩余量与占用率。
- `BookingContextSummary`：采用 `BookingDetailsSheet.tsx` 的主题、用户时区、地点、参与人和重复信息组织。
- `BookingStatusTabs`：采用 `useBookingStatusTab.ts` 的 upcoming、unconfirmed、recurring、past、cancelled 稳定键，并在切换时保留其他查询条件。
- `BookingDirectoryFilter`：采用 `useBookingFilters.ts` 的 eventType、team、user 多选以及独立日期、参与人和 UID 查询通道。
- `BookingDecisionBar`：只处理 PENDING 预约；已付款才能确认，拒绝需要原因，重复预约携带 series 范围。

固定参考 `grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe`：

- `AlertRuleCommandHeader`：采用 `RuleActionsButtons.tsx` 的 edit ability、provisioned provenance、silenceable rule 和 pause/resume 互斥逻辑。
- `AlertStateMetrics`：采用 `triage/scene/SummaryStats.tsx`，规则数按 UID 去重，实例数按真实行计数。
- `AlertInstanceSummary`：采用 `AlertInstanceDetails.tsx`，当前值、清洗后的 annotations、标签和开始时间属于同一实例。
- `AlertMatcherFilter`：采用 `alert-groups/MatcherFilter.tsx` 与 `utils/matchers.ts`，支持 `=`、`!=`、`=~`、`!~`，非法空格和未加双引号的值不能提交。
- `DashboardSaveBar`：采用 `dashboard-scene/scene/new-toolbar/actions/SaveDashboard.tsx`，dirty、canSave、managed、save copy 和 template 能力共同决定保存出口。

固定参考 `marmelab/react-admin@051f511bb0afb5ea565c2d3728bf4dab0a6fa5e0`：

- `ValidatedFormTabs`：采用 `TabbedFormView.tsx` 和 `FormTabHeader.tsx`，所有分区保持参与整表验证，当前项只控制可见性，错误 tab 显式高亮。

桌面使用 Ant Design Button、Popconfirm、Statistic、Descriptions、Tabs、Select、DatePicker、Modal 和 Dropdown；手机端分别翻译为 Grid、Popup、List、Tabs、Selector、CalendarPicker、TextArea 和移动按钮组，没有复用桌面弹层、描述列表或多选下拉。

## 第十二批：工作项、文档与查询区块

本批目录由 111 增至 125，只补七个稀缺区域，每个区域增加两个区块；没有向 `main`、`supplement`、`aside` 或 `overlay` 开放新区块。为核对真实实现，四个参考仓库以 shallow sparse checkout 放在 `C:\Users\wangchunji\Documents\whybuddy-oss-blocks`，不进入 WhyBuddy 仓库。

### Plane 工作项与周期

固定参考 `makeplane/plane@31853ab2b8b7810c59dc30d22e52c8f4b5a71a47`：

- `WorkItemCommandHeader`：参考 `apps/web/core/components/issues/header.tsx` 与 `issues/peek-overview/header.tsx`，标题、状态、优先级和负责人共享同一上下文；活动项可归档，关闭项只开放重新打开。
- `WorkItemContextSummary`：从详情页头提炼项目、周期、截止时间和标签摘要，不复制完整正文。
- `WorkItemDetailTabs`：详情分区使用稳定键，计数不参与身份判断，不可用分区禁用。
- `WorkItemFilterBar`：参考 `issue-layouts/filters/header/filters` 与 `applied-filters`，状态、优先级、负责人和标签各自保留多选状态，清除一个分面不影响其他分面。
- `CycleHealthMetrics`：参考 `cycles/active-cycle/cycle-stats.tsx` 与 `progress.tsx`，完成数、总数、逾期和未开始来自同一周期快照，完成率确定性派生。

### Outline 文档状态机

固定参考 `outline/outline@2cc0d60ed876193b423dd5a103ee4f765c979968`：

- `DocumentCommandHeader`：参考 `app/scenes/Document/components/Header.tsx`，草稿保存、发布、完成编辑和恢复修订是互斥出口。
- `DocumentContextSummary`：参考 `SharedHeader.tsx` 与 `DocumentContext.tsx`，集合、所有者、可见性和更新时间保持在正文之前。
- `BulkSelectionBar`：参考 `app/components/DocumentSelectionToolbar.tsx`，批量动作只读取当前选择集，删除需要确认。
- `DraftPublishBar`：参考 `app/scenes/DocumentPublish.tsx`，没有发布位置或权限时禁止发布；保存草稿不冒充已发布。

### Metabase 查询与参数

固定参考 `metabase/metabase@4dc84c1615d881b69a3a1cfae5bdb46c98a6b8ac`：

- `DataFreshnessIndicator`：参考查询执行时间与 Dashboard 参数刷新路径，真实更新时间和缓存/实时来源分开表达，刷新不在前端伪造完成时间。
- `QueryModeTabs`：查询编辑、结果、可视化和 SQL 使用稳定模式键，切换模式不会重新执行查询。
- `DashboardParameterBar`：参考 `DashboardParameterList.tsx` 与 `ParametersList`，多个参数先形成草稿，必填满足后统一应用。
- `QueryExecutionMetrics`：参考 `ExecutionTime.tsx`，耗时、结果行数、扫描字节和缓存来源来自同一查询结果摘要。

### Backstage 实体处理状态

固定参考 `backstage/backstage@25463a867ce73ad4bd14179889f84cd815affbb7`：

- `EnvironmentStatusStrip`：参考 `EntityProcessingStatusPicker.tsx`、`EntityHeader` 与 `HeaderActionMenu`，状态只读投影为健康、警告和错误，选择环境复用 `itemSelect`，不直接修改实体。

本批严格区域分布为：`header +2`、`headerExtra +2`、`headerContent +2`、`tabs +2`、`filters +2`、`metrics +2`、`footerBar +2`。桌面端使用 Ant Design，手机端使用 Ant Design Mobile 独立实现。事件只使用现有 `itemSelect/actionTrigger/filterChange/editRequest/submitRequest`，没有修改 regions、binding、events、targets、Gate 或模板装配链路。

## 第十三批：目录、协作与查询工作区

本批目录由 125 增至 139，仍只补 `header/headerExtra/headerContent/tabs/filters/metrics/footerBar` 七个偏少区域，每个区域增加两个区块；`main/supplement/aside/overlay` 保持不变。参考仓库继续放在 WhyBuddy 仓库外的 `C:\Users\wangchunji\Documents\whybuddy-oss-blocks`。

### Backstage 软件目录

固定参考：`backstage/backstage@25463a867ce73ad4bd14179889f84cd815affbb7`。

- `CatalogEntityCommandHeader`：参考 `plugins/catalog/src/alpha/components/EntityHeader/EntityHeaderBui.tsx`，标题、kind/type、收藏和上下文菜单共享同一实体引用。
- `EntityOwnershipSummary`：同一源码中的 owner、lifecycle 和 part-of 元数据保持在同一实体上下文，缺失层级不伪造。
- `CatalogEntityFilterBar`：参考 `plugins/catalog-react/src/filters.ts` 与 `CatalogFilterLayout.tsx`，跨分面 AND、分面内多值 OR，owned/starred/all 保持不同身份。

### Outline 文档协作

固定参考：`outline/outline@2cc0d60ed876193b423dd5a103ee4f765c979968`。

- `CollaboratorPresenceStrip`：参考 `app/components/Collaborators.tsx`，在线集合与编辑集合分别计算，在线成员优先，头像溢出保留总人数。
- `DocumentHistoryTabs`：参考 `app/scenes/Document/components/History/History.tsx` 与 `HighlightChangesControl.tsx`，修订、事件和变更对照使用稳定模式键。
- `DocumentInsightMetrics`：参考 `app/scenes/Document/components/Insights.tsx` 与 `DocumentViews.tsx`，阅读、贡献者、创建和更新时间来自同一文档快照。
- `DocumentShareBar`：参考 `app/scenes/Document/components/ShareButton.tsx` 与 `app/components/Sharing/Document`，公开状态、权限、域名和真实链接共同决定分享出口。

### Metabase 查询工作区

固定参考：`metabase/metabase@4dc84c1615d881b69a3a1cfae5bdb46c98a6b8ac`。

- `QuestionCommandHeader`：参考 `ViewTitleHeader.tsx` 和 `QuestionActions`，临时/已保存、脏状态、收藏和复制从同一问题状态派生。
- `QueryRunStatusStrip`：参考 `ViewTitleHeader.tsx` 与 `ExecutionTime.tsx`，运行和取消互斥，耗时与缓存来源读取真实结果。
- `QueryDataSourceSummary`：参考 `QuestionDataSource.tsx` 与 `DataSourceCrumbs.tsx`，数据库、Schema 和源表/模型形成一条稳定来源链。
- `DatasetEditorTabs`：参考 `DatasetEditor/EditorTabs/EditorTabs.tsx`，query、columns、metadata 使用稳定键，不可用能力保持禁用。
- `QueryClauseFilterBar`：参考 `QuestionFiltersHeader.tsx`，结构化条件逐条回显和撤销，单条移除不清空其他条件。
- `MetadataQualityMetrics`：参考 `DatasetMetadataStrengthIndicator.tsx`，完整度从字段覆盖机械计算，不保存第二份视觉分数。
- `QuestionExecutionBar`：参考 `ViewTitleHeaderRightSide.tsx`，`isRunnable/isRunning/isDirty` 共同决定运行、取消和保存出口。

本批仍严格分布为 `header +2`、`headerExtra +2`、`headerContent +2`、`tabs +2`、`filters +2`、`metrics +2`、`footerBar +2`。桌面端使用 Ant Design；手机端使用 Ant Design Mobile 的 Popup、Selector、Tabs、List、Grid、ProgressBar 和移动按钮组独立实现，没有桌面组件降级，也没有扩展契约词表。

## 第十四批：周期、事故与运行可靠性

本批目录由 139 增至 153，继续只补七个非主体区域，每个区域两个区块；没有向 `main/supplement/aside/overlay` 增加投放，也没有修改 binding、events、targets、Gate 或模板装配链路。

### Plane 周期管理

固定参考：`makeplane/plane@31853ab2b8b7810c59dc30d22e52c8f4b5a71a47`。

- `CycleCommandHeader`：参考 `cycles/quick-actions.tsx`，周期编辑、复制链接、归档和恢复由周期状态与项目权限共同决定。
- `CycleContextSummary`：参考 `cycles/analytics-sidebar/sidebar-details.tsx`，负责人、成员、日期和工作项进度属于同一周期快照；完成周期优先读取快照。
- `CycleFilterBar`：参考 `cycles/dropdowns/filters`、`applied-filters` 和 `cycles-view-header.tsx`，状态与日期分面独立累计，搜索保持另一条通道。
- `CycleLifecycleBar`：参考归档、恢复和删除弹层，活动周期可完成/归档，只有已归档周期允许确认删除。

### Sentry 事故处置

固定参考：`getsentry/sentry@b815e2e0b42465e6cac11aa7e7717d96863a9120`。

- `IncidentOwnershipStrip`：参考 `issueDetails/header/assigneeSelector.tsx` 与 `getOwnerList.tsx`，当前负责人、自动归属来源和候选负责人分开表达，建议不会覆盖已有负责人。
- `IncidentEvidenceTabs`：参考 Issue Header、附件和证据抽屉，事件、附件、回放、用户反馈只在真实可用时开放。

### Airbyte 同步计划

固定参考：`airbytehq/airbyte-platform@59be68e17070ac655de596507d73480ed02b61a2`。

- `SyncScheduleStrip`：参考 `ConnectionPageHeader.tsx` 和连接设置页，频率、下次运行、时区与暂停状态共享同一连接计划。
- `SyncReliabilityMetrics`：参考 `JobStats.tsx` 和 `StreamStatusPage`，成功率从成功/失败运行机械计算，记录量和新鲜度读取同一运行快照。

### Grafana 规则组与评估

固定参考：`grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe`。

- `AlertGroupCommandHeader`：参考 Group Details、评估间隔和 Rule Action Buttons，暂停/恢复与编辑权限保持一致。
- `AlertGroupContextSummary`：namespace、group、评估间隔和规则数不脱离规则组上下文。
- `AlertRuleFilterBar`：参考 `RulesFilter.v2.tsx` 与规则查询解析器，支持名称、数据源、命名空间、标签、状态、类型和健康度语法；分组/列表视图与查询值分离。
- `RuleEvaluationMetrics`：参考 `RulesByEvaluationPercentage.tsx` 与 `ruleStats.ts`，活跃、暂停、错误和平均耗时使用同一评估口径。

### Cal.com 事件类型编辑

固定参考：`calcom/cal.com@176037d0afbe572f870a3c702985e7cd83fe6c0c`。

- `EventTypeEditorTabs`：参考 `EventType.tsx` 与 `EventTypeLayout.tsx`，桌面纵向和窄屏横向页签共享设置、可用时间、限制、高级、应用及 Webhook 的稳定能力键。
- `EventTypePublishBar`：hidden、dirty、valid 和公开资料开关分别控制保存与发布；保存不冒充已经公开。

本批严格分布仍是 `header +2`、`headerExtra +2`、`headerContent +2`、`tabs +2`、`filters +2`、`metrics +2`、`footerBar +2`。桌面端使用 Ant Design，手机端使用 Ant Design Mobile 独立实现。

## 第二十二批：侧栏、抽屉与上下文任务面

固定参考版本：`ant-design/ant-design-pro@bb40867ae3ef943e06e48da6911ccc5900e638a1`、`ant-design/pro-components@358de7cdd602b68dad491ef82e93661a96c42901`、`ToolJet/ToolJet@dea53c36391fb9cc49a707ee031fe6b5cb40ebf8`。ToolJet 浅克隆位于仓库外 `C:\Users\wangchunji\Documents\codex-references\tooljet`。

- `KeyboardCommandPalette`：参考 ToolJet `frontend/src/AppBuilder/LeftSidebar/LeftSidebarInspector` 和 `_ui/Search/Search.jsx`，搜索只缩小候选，选择命令才发执行事件。
- `NotificationCenterDrawer`：参考 ToolJet `frontend/src/_components/NotificationCenter/{index,NotificationRow}.jsx`，打开抽屉不自动清空未读，只对未读行开放标记已读。
- `FilterPresetDrawer`、`SavedSearchPanel`：参考 ProComponents `src/form/layouts/QueryFilter/index.tsx`，本地选择与最终应用分离；运行保存搜索不修改保存定义。
- `ExportJobDrawer`：参考 ToolJet `frontend/src/HomePage/ExportAppModal.jsx`，只有 running 可取消，只有 completed 可下载。
- `CompareSelectionTray`、`SelectionInspector`：参考 ProComponents `src/table/components/Alert/index.tsx`，选择集合是唯一计数来源，对比严格要求两条记录。
- `DetailInspectorDrawer`、`RelatedEntityPanel`：参考 ToolJet Inspector，详情只读且关闭不改变选择；关联实体按真实关系字段分组。
- `HelpContextPanel`、`ContextHelpDrawer`：帮助主题折叠与搜索分离，搜索限定当前页面上下文，不修改业务数据。
- `AuditDiffDrawer`：参考 ToolJet `frontend/src/_services/auditLogsService.js` 和 `docs/docs/security/audit-logs.md`，审计行按真实时间倒序且不可编辑。
- `RecentItemsPanel`：参考 Ant Design Pro `src/pages/dashboard/workplace`，只按真实访问时间排序，最多展示十项。
- `PermissionSummaryPanel`：参考 ToolJet `ManageGroupPermissionResources`，权限摘要只读，权限申请不在前端直接提权。
- `ValidationIssuePanel`：参考 ProComponents `DrawerForm` 与校验测试，错误阻止提交，警告只提示，失败时保持任务面可见。
- `ChangeImpactPanel`：影响分析只读，高风险需要明确确认，确认不冒充变更已经执行。

本批只使用既有 `entityRows/selection`、字段引用、`itemSelect/actionTrigger/filterChange/submitRequest`、`targets` 和现有 `aside/overlay/supplement/footerBar` 区域，没有扩充契约词表。桌面使用 Ant Design Drawer/List/Collapse/Alert，手机使用 Ant Design Mobile Popup/List/Collapse/SearchBar 独立实现。

## 第十五批：客服会话与身份权限

本批目录由 153 增至 167。新增参考仓库以 shallow sparse checkout 放在 WhyBuddy 仓库外：`chatwoot/chatwoot@a4eae9710ab49e7dc98d64a7e713fd1f77c451f8` 与 `keycloak/keycloak@f03a2104ec6e1fa0b463d71ecb2532a51b228dfe`。

### Chatwoot 客服会话

- `ConversationCommandHeader`：参考 `ConversationHeader.vue` 和 `MoreActions.vue`，联系人、收件箱、HMAC 验证、暂停、SLA 与会话状态共享同一上下文；解决和重新打开互斥。
- `ConversationAssignmentStrip`：参考 `ConversationAction.vue`，处理人、团队和优先级独立更新，分配给我不覆盖其他字段。
- `ConversationContextSummary`：联系人、渠道、收件箱、联系电话和 SLA 在消息正文之前形成紧凑上下文。
- `ConversationDetailTabs`：消息、活动、联系人和附件使用稳定键，能力不可用时禁用。
- `ConversationInboxFilter`：参考 `ConversationFilter.vue` 和服务端 `filter_service.rb`，状态、收件箱、处理人、团队、标签和优先级分面与后端语义对齐。
- `ConversationSlaMetrics`：参考 `SLAMetrics.vue` 和 `SLAMetricCard.vue`，首次响应、解决时间、违约数和会话数使用同一报告范围。
- `ConversationReplyBar`：参考 `ReplyBox.vue`，公开回复和内部备注互斥，关闭会话不能发送，草稿清理只发生在真实提交后。

### Keycloak 身份管理

- `UserCommandHeader`：参考 `EditUser.tsx`，启停需要确认，模拟登录使用独立权限，启停不等于删除。
- `RealmStatusStrip`：参考 `GeneralTab.tsx` 与 `BruteForceDetection.tsx`，Realm 启用、SSL 要求和暴力破解防护保持同一配置快照。
- `UserIdentitySummary`：参考 `UserForm.tsx` 与 `FederatedUserLink.tsx`，用户名、邮箱、联邦来源、创建时间、验证状态和必需动作属于同一用户。
- `UserSecurityTabs`：参考 `EditUser.tsx` 的 RoutableTabs，详情、凭据、角色、组、身份提供方、会话和事件按权限开放。
- `UserDirectoryFilter`：参考 `UserDataTable.tsx` 与 Toolbar，默认用户名搜索和属性查询是互斥模式；切换模式清除旧查询。
- `UserSessionMetrics`：参考 `UserSessions.tsx` 与 `SessionsTable.tsx`，在线/离线会话、客户端和风险会话读取真实 session 快照。
- `UserAccessBar`：重置凭据、注销全部会话和启停用户保持不同安全边界；没有管理权限时全部禁用。

本批仍严格按照七个偏少区域各增加两个区块，不增加 `main/supplement/aside/overlay`，也不修改 regions、binding、events、targets、Gate 或模板装配链路。桌面端使用 Ant Design，手机端使用 Ant Design Mobile 独立实现。

## 第十六批：分析图表与运行上下文

本批目录由 167 增至 179，优先补齐数量最少且内部形态仍单一的 `charts/tabs/metrics/headerContent/headerExtra/header`。参考源码继续使用仓库外的固定浅克隆，不把第三方源码带入 WhyBuddy。

- `grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe`：`TimeSeriesAnomalyChart` 保留空值断点，异常只由真实标记字段产生，期望上下界与观测值共用同一时间轴。
- `metabase/metabase@4dc84c1615d881b69a3a1cfae5bdb46c98a6b8ac`：`CohortRetentionChart` 区分缺失组合和有效的 0%；`DashboardQueryContext` 保留当前数据源、筛选和缓存口径；`DashboardCommandHeader` 把收藏、订阅、刷新和编辑拆成独立能力。
- `airbytehq/airbyte-platform@59be68e17070ac655de596507d73480ed02b61a2`：`UptimeStatusTimeline` 保留未知窗口；`ConnectionWorkspaceTabs` 使用稳定能力键；`ConnectionFleetMetrics` 让每条连接只进入一个状态桶，并可机械下钻筛选。
- `getsentry/sentry@b815e2e0b42465e6cac11aa7e7717d96863a9120`：`PercentileBandChart` 让 P50/P95/P99 共用时间桶；`IssueInvestigationTabs` 按真实能力开放证据页签；`IssueImpactMetrics` 与 `ReleaseHealthStrip` 都只读取同一范围快照。
- `backstage/backstage@25463a867ce73ad4bd14179889f84cd815affbb7`：`ServiceOwnershipContext` 将 owner、system、lifecycle 等关系约束在当前目录实体内，不跨实体猜测。

本批新增 `charts +4`、`tabs +2`、`metrics +2`、`headerContent +2`、`headerExtra +1`、`header +1`。没有增加 `main/supplement/aside/overlay`，没有新增区域、binding 语法、事件类型、targets、Gate 或模板装配机制。桌面端使用 Ant Design/ECharts，手机端使用 Ant Design Mobile 与独立图表布局。

## 第十七批：部署运行与发布灰度

本批目录由 179 增至 195，继续补齐稀缺区域，但把样本转向两套完整状态机，而不是继续复制信息页头。固定参考 `backstage/backstage@25463a867ce73ad4bd14179889f84cd815affbb7` 的 Kubernetes 插件，以及 `getsentry/sentry@b815e2e0b42465e6cac11aa7e7717d96863a9120` 的 releases、release health 与 feature flags 页面。

### Backstage Kubernetes 部署组合

- `DeploymentLatencyChart`：排队、拉取镜像、启动和就绪阶段共用部署时间桶；未开始阶段保持断点。
- `DeploymentDetailTabs`：摘要、Pod、事件、日志和配置使用稳定能力键，没有能力时禁用。
- `DeploymentRolloutMetrics`：desired、ready、available 和 unavailable 从同一 workload 快照读取，desired 为 0 时不伪造健康。
- `ClusterHealthStrip`：未知和未连接集群不显示为健康。
- `DeploymentContextSummary`：命名空间、集群、镜像和滚动策略只读取当前 workload。
- `KubernetesResourceFilter`：集群、命名空间、资源类型和状态分面累计生效。
- `DeploymentCommandHeader`：日志、编辑和滚动重启是三条独立能力，重启必须确认。
- `DeploymentScaleBar`：当前就绪只读，只提交目标副本；真实 rollout 进度仍由宿主刷新。

### Sentry 发布与灰度组合

- `ReleaseAdoptionTrendChart`：采用率与无崩溃率共享版本、环境和时间范围。
- `ReleaseDetailTabs`：概览、错误、事务、用户和环境切换不丢失 release 上下文。
- `ReleaseAdoptionMetrics`：采用率、健康率、事件数和用户数来自同一发布快照。
- `ReleaseEnvironmentStrip`：切换环境只改变筛选，不重建版本身份。
- `ReleaseContextSummary`：项目、环境、提交和发布者属于当前 release。
- `ReleaseEnvironmentFilter`：环境、版本和健康状态与文本搜索保持独立。
- `FeatureFlagCommandHeader`：启停与灰度比例是不同写操作，均服从编辑权限。
- `ReleaseRolloutBar`：暂停、继续和完成互斥；采用率未到 100% 或健康率不足时禁止完成。

本批新增 `charts/tabs/metrics/headerExtra/headerContent/filters/header/footerBar` 各两个。仍未修改区域、binding、events、targets、Gate 或模板装配，也没有为了数量扩充 `main/supplement/aside/overlay`。桌面端使用 Ant Design/ECharts，手机端使用 Ant Design Mobile 独立实现。

## 第十八批：工作流分析与预约冲突任务

本批目录由 195 增至 205，只补当时数量最低的 `charts/tabs/metrics/headerExtra/overlay`，每个区域增加两个。固定参考 `makeplane/plane@31853ab2b8b7810c59dc30d22e52c8f4b5a71a47` 与 `calcom/cal.com@176037d0afbe572f870a3c702985e7cd83fe6c0c`。

- `CumulativeFlowChart`：状态按日期桶堆叠，展示在制品积压；缺失日期不补造。
- `BookingDemandChart`：可用、预约和取消使用同一日期与时区口径。
- `WorkItemActivityTabs`、`BookingAuditTabs`：证据页签使用稳定键，能力不可用时禁用。
- `WorkloadThroughputMetrics`：完成、进入、在制和阻塞来自同一周期；进入量为零时完成率为零。
- `CalendarUtilizationMetrics`：利用率由已预约分钟除以可用分钟机械计算，取消和未到场独立。
- `CycleRiskStrip`：剩余天数、阻塞和超期共同说明周期风险。
- `CalendarConnectionStrip`：连接分别维护同步状态，失败重试只携带当前连接 id。
- `WorkItemMoveDrawer`：目标分组先在本地选择，确认后一次提交真实工作项 id。
- `BookingConflictDrawer`：只展示真实可用性冲突，改期仍交给宿主流程，不伪造时段。

两个 overlay 区块在桌面端使用 Ant Design Drawer，在手机端使用 Ant Design Mobile Popup。仍未增加区域、binding、事件或 Gate 词汇。

## 第十九批：工作流运行与 Realm 安全

本批目录由 205 增至 218，将九个非主体区域全部收敛到 22。固定参考 `nocobase/nocobase@43961f62b3526eda07e080235002ae9ee23c61a7` 与 `keycloak/keycloak@f03a2104ec6e1fa0b463d71ecb2532a51b228dfe`。

NocoBase 工作流链新增 `WorkflowDurationChart`、`WorkflowExecutionTabs`、`WorkflowOutcomeMetrics`、`WorkflowVersionStrip`、`WorkflowFailureDrawer`、`WorkflowCommandHeader`、`WorkflowContextSummary`、`WorkflowExecutionFilter` 与 `WorkflowControlBar`。实现保留 started/resolved/failed/aborted 等状态边界：运行中才可取消，失败或中止才可重试，成功只读；停用工作流只阻止新执行，不取消已启动 execution。

Keycloak 安全链新增 `RealmCommandHeader`、`RealmSecurityContext`、`UserEventFilter` 与 `CredentialLifecycleBar`。启停 Realm 需要确认且不等于删除；安全摘要不跨 Realm 拼接；用户事件分面累计；本地重置密码、发送重置邮件和要求下次更新是不同事务，联邦凭据不可本地重置。

没有增加任何区域、binding、events、targets 或 Gate 词汇。桌面失败诊断使用 Ant Design Drawer，手机使用 Ant Design Mobile Popup。

## 第二十批：查询诊断与 Schema 同步

本批目录由 218 增至 236，九个非主体区域各增加两个并全部达到 24。固定参考 `grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe` 的 QueryInspector、Explore、Dashboard Inspector，以及 `airbytehq/airbyte-platform@59be68e17070ac655de596507d73480ed02b61a2` 的 StreamStatusPage、SyncCatalogTable、SchemaRefreshing 和 ConnectionTimeline。

Grafana 链新增面板查询延迟、检查页签、数据源查询指标、数据源健康、面板页头、Explore 查询摘要、日志标签筛选、查询控制和查询错误抽屉。Airbyte 链新增同步数据量、数据流页签、新鲜度指标、连接器版本、Schema 页头、选择摘要、命名空间筛选、Schema 刷新栏和冲突抽屉。刷新、检查、编辑、取消、重试和保存均保持独立 operation；breaking Schema 必须先审查，刷新中禁止重复刷新。

没有增加区域、binding、events、targets 或 Gate 词汇。桌面诊断使用 Drawer，手机使用 Popup。

## 第二十一批：稀缺看板与配置向导

本批补齐 12 个看板形态与 4 个向导形态，固定参考仓库版本：`makeplane/plane@31853ab2b8b7810c59dc30d22e52c8f4b5a71a47`、`nocobase/nocobase@43961f62b3526eda07e080235002ae9ee23c61a7`、`ant-design/pro-components@358de7cdd602b68dad491ef82e93661a96c42901`。参考源码位于仓库外 `C:\Users\wangchunji\Documents\whybuddy-oss-blocks`，不进入 WhyBuddy。

### 看板内部逻辑

- `SwimlaneKanban`、`ContentPipelineBoard`、`ReleaseTrainBoard`、`PortfolioKanban`：对应 Plane `apps/web/core/components/issues/issue-layouts/kanban/swimlanes.tsx`，泳道身份与状态列分开，空泳道保留，移动事件携带源列和目标列。
- `WipLimitBoard`、`SprintPlanningBoard`：对应 Plane `kanban-group.tsx` 与 cycle 根视图，列头显示当前数量/限制，达到上限时目标阶段禁用。
- `BacklogPrioritizationBoard`、`TriageQueueBoard`、`IncidentResponseBoard`：保留稳定优先级排序，选择和分组互不覆盖；阻塞记录仍可查看但不能推进。
- `DependencyKanban`：卡片的依赖阻塞是显式状态，未解除依赖时移动控件禁用。
- `ApprovalStageBoard`、`RecruitmentPipelineBoard`：阶段推进使用确认边界，确认前只修改局部选择，不直接伪造持久化结果。

### 向导内部逻辑

- `ImportMappingWizard`：对应 NocoBase `packages/plugins/@nocobase/plugin-action-import/src/client/ImportModal.tsx`，每步的映射是本地草稿，映射缺失或错误时不能前进，最后才提交完整映射集合。
- `OnboardingChecklistWizard`、`IntegrationSetupWizard`：对应 ProComponents `src/form/layouts/StepsForm/index.tsx`，步骤状态、进度和校验共享，异步失败停留在当前步骤。
- `PolicyConfigurationWizard`：对应 NocoBase workflow client，草稿校验与最终启用分离，错误/阻塞规则不能被完成动作绕过。

桌面端使用 Ant Design/ProComponents 的 Card、Steps、Progress、Select、Alert、Popconfirm；手机端使用 Ant Design Mobile 的 Card、Steps、Selector、ProgressBar、ErrorBlock、Dialog。16 个区块只复用现有 `entityRows`、字段引用、`itemSelect`、`editRequest`、`stepChange`、`submitRequest` 与 `targets`，没有增加区域、binding 语法、事件词、Gate 或模板装配链路。

## 第二十三批：排期日历与预约状态任务

本批固定参考 `calcom/cal.com@176037d0afbe572f870a3c702985e7cd83fe6c0c`、`makeplane/plane@31853ab2b8b7810c59dc30d22e52c8f4b5a71a47` 和 `ant-design-mobile@df8220353d17e1aa22c7a078cc7808e84a82c0c3`。

- Cal.com `packages/features/schedules/components/ScheduleComponent.tsx`、`DateOverrideList.tsx`、`DateOverrideInputDialog.tsx`：重复窗口与日期覆盖共用排期状态；覆盖冲突阻止保存，空窗口明确表示整日不可用。
- Cal.com `packages/features/schedules/lib/slots.ts`、`date-ranges.ts`、`hooks/useTimesForSchedule.ts`：可选时段由可用区间扣除忙碌区间产生；时区转换保留绝对时刻，跨午夜窗口正确拆分。
- Cal.com `packages/features/bookings/lib/conflictChecker/checkForConflicts.ts`、`apps/web/test/lib/getSchedule/calendarEvents.test.ts`：内部预约与外部日历事件共同参与冲突判断，前端不根据颜色自行宣称时段可用。
- Cal.com `apps/web/app/reschedule/[uid]/page.tsx`、`packages/features/bookings/lib/reschedule/determineReschedulePreventionRedirect.ts`：改期保留原预约上下文；无权限、已取消和超过最小通知边界时阻止继续。
- Plane `apps/web/core/components/issues/issue-layouts/calendar/calendar.tsx`、`day-tile.tsx`、`issue-block.tsx`：月份导航、日期格、任务选择与快速创建共享日期上下文，但保持独立事件。
- Ant Design Mobile `src/components/calendar-picker/calendar-picker.tsx`、`popup/popup.tsx`、`list`：手机端使用触摸日期选择、纵向任务列表和底部临时任务面，不复用桌面 Calendar/Drawer 结构。

落地区块包括 11 个日历：`ServiceDispatchCalendar`、`InterviewScheduleCalendar`、`ExamScheduleCalendar`、`TrainingCalendar`、`LeaveCalendar`、`AssetReservationCalendar`、`RoomBookingCalendar`、`DeliverySlotCalendar`、`OnCallScheduleCalendar`、`ProductionScheduleCalendar`、`MilestoneTimelineCalendar`；以及 5 个状态任务：`AppointmentWaitlistPanel`、`AvailabilityOverridePanel`、`TimezoneOverlapPanel`、`SchedulePublishBar`、`RescheduleRequestDrawer`。它们只复用现有区域、字段引用、事件和 targets，没有扩展 Gate 或模板装配词表。

## 第二十四批：高级配置与审批向导

本批新增 16 个有明确终局动作的配置向导。固定参考版本为 `pro-components@358de7cdd602b68dad491ef82e93661a96c42901`、`nocobase@43961f62b3526eda07e080235002ae9ee23c61a7`、`keycloak@f03a2104ec6e1fa0b463d71ecb2532a51b228dfe`、`airbyte-platform@59be68e17070ac655de596507d73480ed02b61a2` 与 `ant-design-pro@bb40867ae3ef943e06e48da6911ccc5900e638a1`。

- `BulkOperationWizard` 对应 NocoBase `plugin-action-bulk-edit/src/client-v2/submitHandler.ts` 与 `plugin-action-bulk-update/src/client-v2/BulkUpdateActionModel.tsx`：选择集合与字段草稿分离，没有选择时阻塞，只提交真实 row id。
- `DataQualityRepairWizard`、`ConnectorMigrationWizard` 对应 Airbyte `src/components/ui/forms/SchemaForm/dynamicValidator.ts`、`src/area/connection/components/CreateConnectionForm/CreateConnectionForm.tsx` 和 `ConnectionTimelinePage/components/ConnectorUpdateEventItem.tsx`：校验问题先汇总，兼容性和回滚计划在最终动作前确认。
- `AccessRequestWizard`、`ApiCredentialWizard`、`RoleAssignmentWizard` 对应 Keycloak `js/apps/admin-ui/src/user/UserRoleMapping.tsx`、`UserCredentials.tsx`、`user-credentials/ResetCredentialDialog.tsx`：申请、凭据轮换和授权请求与最终提交分离，不在前端伪造授权成功。
- `SurveyBuilderWizard`、`WorkspaceCreationWizard`、`CheckoutReviewWizard`、`ReportScheduleWizard`、`RecoveryPlanWizard` 与 `ComplianceAttestationWizard` 提炼 ProComponents `src/form/layouts/StepsForm/index.tsx`、`StepForm.tsx`、Ant Design Pro `src/pages/form/step-form/index.tsx` 和 `advanced-form/index.tsx`：步骤草稿持续保留，异步失败停留当前步骤，最后一步统一确认。
- `ApprovalRoutingWizard`、`DataRetentionWizard`、`NotificationRuleWizard`、`LocalizationSetupWizard` 分别提炼 NocoBase workflow client、ProComponents StepsForm 与 Keycloak `js/apps/admin-ui/src/realm-settings/localization/LocalizationTab.tsx`：规则编辑、风险审查、启用/保存是独立动作，阻塞状态不能绕过。

桌面端是独立 Ant Design + ProForm/Steps 渲染器，手机端是独立 Ant Design Mobile Steps/Selector/TextArea/Checkbox/ProgressBar/ErrorBlock/Dialog 渲染器；二者共享的只有策略、字段引用和宿主事件。16 个区块只使用已有 `entityRows`、`entityRef/titleFieldRef/statusFieldRef/descFieldRef/targets`，事件只使用 `stepChange` 与 `submitRequest`，没有修改 regions、binding 语法、targets、Gate 或模板装配链路。
### 分析与依赖区块：13 个真实渲染实体

本批只扩充区块内部组合，不改变 WhyBuddy 的 regions、binding、events、targets、Gate 或模板装配契约。参考仓库固定版本：

- `apache/superset`（本地 `whybuddy-oss-blocks/superset`，HEAD `3b164e4270860ac07223d4df1a60ca7b56312362`）：`superset-frontend/plugins/plugin-chart-echarts/src/Funnel/EchartsFunnel.tsx`、`Histogram/Histogram.tsx`、`Waterfall/EchartsWaterfall.tsx`。采用阶段顺序、固定箱数、增减累积和空值过滤。
- `grafana/grafana`（本地 `whybuddy-oss-blocks/grafana`，HEAD `eec4a1c66b328851863369c9e031931c9b83cdbe`）：node graph/timeseries 面板逻辑。采用真实 source-target 边、禁止自环、关系图与时间序列分层。
- `metabase/metabase`（本地 `whybuddy-oss-blocks/metabase`，HEAD `4dc84c1615d881b69a3a1cfae5bdb46c98a6b8ac`）：query builder chart selector 与结果表逻辑。采用成对数值散点和透视交叉单元缺失不补零。
- `getsentry/sentry`（本地 `whybuddy-oss-blocks/sentry`，HEAD `b815e2e0b42465e6cac11aa7e7717d96863a9120`）：性能/告警关系面板。采用预算分母为零时空态、状态关系不改写原始数据。
- `makeplane/plane`（本地 `whybuddy-oss-blocks/plane`，HEAD `31853ab2b8b7810c59dc30d22e52c8f4b5a71a47`）：`apps/web/core/components/cycles/active-cycle/cycle-stats.tsx`。采用 burnup/burndown 的完成线、范围线、剩余线独立计算。

新增区块：`FunnelConversionChart`、`HistogramDistributionChart`、`ScatterCorrelationChart`、`BoxPlotDistributionChart`、`WaterfallVarianceChart`、`ForecastConfidenceChart`、`BurnupChart`、`BurndownChart`、`ErrorBudgetGauge`、`ServiceMapPanel`、`DependencyGraphPanel`、`QueryResultPivot`、`MetricComparisonPanel`。桌面端使用 Ant Design + ECharts，手机端使用 Ant Design Mobile 外壳 + 独立 ECharts 渲染；所有区块均从真实 `entityRef`/字段 binding 取数，空值、空态和缺字段均有明确边界。

## 第二十五批：协作内容与数据治理（24 个）

本批继续沿用现有区域、binding、events、targets、Gate 和模板装配链，只提炼区块内部协作逻辑。开源样本来自本地固定版本仓库：Outline 的 mention、文档大纲和 backlinks；Chatwoot 的回应、讨论解决和在线成员；Plane 的版本/关注者协作；Backstage 的目录图、责任与权限边界；Airbyte 的 Schema 映射、检查点、失败重试和任务历史；NocoBase 的导入/合并流程；ToolJet 的转换预览、变更检查和权限资源管理；Sentry/Grafana 的投递与升级策略。

新增协作内容区块：`MentionComposer`、`ReactionSummary`、`ThreadResolutionBar`、`DocumentOutlinePanel`、`VersionComparePanel`、`ApprovalCommentPanel`、`KnowledgeBacklinkPanel`、`PresenceRosterPanel`、`AssignmentQueuePanel`、`EscalationPolicyPanel`、`OwnershipTransferPanel`、`WatcherManagerPanel`。

新增数据治理区块：`SchemaMappingPanel`、`FieldTransformPanel`、`ImportValidationPanel`、`DuplicateResolutionPanel`、`MergePreviewPanel`、`SyncCheckpointPanel`、`RetryQueuePanel`、`JobRunHistoryPanel`、`DataLineagePanel`、`RecordChangePreview`、`PermissionMatrixPanel`、`WebhookDeliveryPanel`。

采用的关键边界：mention 候选与正文草稿分离；评论与审批决策分离；在线状态不冒充编辑锁；版本对比只读且严格选择两版；所有权转移需要确认；关注不改变授权；映射/转换草稿先校验或预览再提交；导入错误阻塞、警告不阻塞；合并和变更预览不冒充已写入；重试载荷只携带失败项 id；权限矩阵只提交申请，不在前端直接提权；Webhook 成功记录与失败重试分离。

桌面实现使用 Ant Design，手机实现使用 Ant Design Mobile 独立渲染模块，没有复用桌面 JSX，也没有新增契约词表。
## 第十九批后续：独立结构族试点（6 个）

本批停止使用“领域 x 固定 variant”矩阵扩充。新区块必须同时提供 `structureFamily` 和 `structureDelta`，并拥有独立桌面渲染函数、独立手机交互、组件库真实 demo 与双端截图。共享业务名称或 operation 不再计作新的结构区块。

- `SignatureFieldCanvas`：参考 `documenso/documenso@HEAD:apps/remix/app/components/general/document-signing`，采用文档画布、签署人轨道与字段定位联动；手机端改为文档/签署人标签页。
- `AlertGroupAccordion`：参考 `grafana/grafana@HEAD:public/app/features/alerting/unified`，采用组级聚合、实例折叠和组级静默，不摊平成告警表格。
- `EvidenceCollectionWorkspace`：参考 `DefectDojo/django-DefectDojo@HEAD:dojo`，组合要求清单、文件待上传队列、完成度和送审阻塞。
- `AssetReviewLightbox`：参考 `immich-app/immich@HEAD:web/src/lib/components/asset-viewer`，采用媒体墙、选择态和详情灯箱；手机详情进入 Popup。
- `PaymentAllocationWorkbench`：参考 `invoiceninja/invoiceninja@HEAD:resources`，采用付款总额约束、逐发票金额分配和剩余余额校验。
- `DeploymentRolloutTrack`：参考 `argoproj/argo-cd@HEAD:ui/src/app/applications/components`，采用顺序发布轨道、健康 Gate 和定点阶段重试。

六个源码目录均通过 `git cat-file -e HEAD:path`。生成后的组件依赖签名六者各不相同，桌面端和手机端均不依赖 `Table`；测试同时要求六个 `structureFamily`、`rendererKey` 唯一，并验证双端真实渲染。组件库截图逐卡检查横向溢出和元信息浮层与交互控件相交，不能用空态截图充当验收结果。

## 独立结构族第二批：调试、查询、空间、实验、SLA 与拣货（6 个）

本批目录由 365 增至 371。每个条目继续要求唯一 `structureFamily`、明确 `structureDelta`、具名桌面渲染器、独立手机交互和真实 demo；6 个区块的组件依赖签名各不相同，桌面与手机依赖均不包含 `Table`。来源均位于仓库外 `C:\Users\wangchunji\Documents\whybuddy-oss-blocks`，并对下列固定提交和路径执行了 `git cat-file -e`：

- `WorkflowNodeDebugger`：参考 `n8n-io/n8n@9205eb3f1908b94904b6d5400c32dcf3c4baf1b7:packages/frontend/editor-ui/src/features/workflows/canvas/components/WorkflowCanvas.vue`。新增节点拓扑、节点选择检查器和失败节点定点重试；桌面使用 XYFlow，手机将画布翻译为纵向节点轨道和选中节点详情。成功节点不能重试，选择态不改写运行状态。
- `QueryNotebookComposer`：参考 `apache/superset@3b164e4270860ac07223d4df1a60ca7b56312362:superset-frontend/src/SqlLab`。新增多查询单元、单元草稿、结果摘要和独立运行；桌面复用项目已有的懒加载 CodeMirror，手机使用折叠单元和 TextArea。提交始终携带当前单元的实时草稿与 rowId，不覆盖其他单元。
- `WarehouseBinHeatmap`：参考 `frappe/erpnext@b55c6d16c90efc862544150d83def414f6d78497:erpnext/stock/doctype/bin`。新增巷道切换、二维仓位热力格和容量状态映射；手机使用两列触摸网格。容量为零时占用率保持 0%，超容量显示上限 100%，选择提交真实仓位 id。
- `ExperimentTrafficAllocator`：参考 `gitlab-org/gitlab@4c7b9936af19984be51a44c779742a0a80a21cb1:app/assets/javascripts/feature_flags/components/strategies/flexible_rollout.vue`。新增多变体 Slider、总量进度和 100% 发布 Gate；手机保持逐变体触摸滑杆。总量不等于 100 时发布按钮禁用，本地滑动不冒充持久化结果。
- `SlaBreachClock`：参考 `zammad/zammad@b7b8f28e3b423e3c440ac29f84f38fc28f389d1a:app/assets/javascripts/app/controllers/_ui_element/sla_times.coffee`。新增最近截止项选择、倒计时仪表、风险时间线和定点升级；手机将仪表翻译为大数字倒计时卡。升级提交当前工单 rowId 与负责人，不直接改写工单状态。
- `WarehousePickRouteScanner`：参考 `frappe/erpnext@b55c6d16c90efc862544150d83def414f6d78497:erpnext/stock/doctype/pick_list/pick_list.js` 的 `BarcodeScanner` 与 `picked_qty` 约束。新增有序仓位轨道、当前项扫码校验和逐项推进；手机保持原生触摸输入与步骤轨道。条码不匹配时不提交、不推进。

真实预览保存在 `artifacts/independent-structure-blocks-batch2`：6 个类型各有桌面和手机截图，共 12 张。自动测量结果为：空态 `0`、卡片横向溢出 `0`、元信息浮层与输入/按钮/滑杆遮挡 `0`。桌面 SLA 的 SVG 圆弧在元素级边界扫描中会因变换坐标产生假阳性，实际卡片 `scrollWidth` 无溢出，截图也未越界。截图门禁与行为测试共同作为计数条件，只有目录记录或共享渲染别名不计入新增结构族。

## 独立结构族第三批：传输、追踪、映射、回放、网格与日志（6 个）

本批目录由 371 增至 377。候选阶段曾考虑继续增加周可用时间编辑器，但 Cal.com `ScheduleComponent` 与现有 `AvailabilityPlanner` 都是“星期 + 时间段列表”结构，因此不计为新区块，最终改为拥有独立回放状态的会话时间轴。本批 6 个类型继续要求唯一 `structureFamily`、明确 `structureDelta`、具名桌面渲染器、手机独立交互、真实 demo、非表格依赖和双端截图。

- `ResumableUploadQueue`：参考 `nextcloud/server@90d06caf85b308cc4579e25619b269f0df22f17b:apps/files/src/store/uploader.ts`。新增拖放入队、逐文件进度、暂停、恢复、失败重试与取消状态机；完成文件不再出现取消上传动作。它与 `AttachmentPanel` 的区别是后者展示已存在附件，本区块管理正在发生的传输。
- `DistributedTraceWaterfall`：参考 `getsentry/sentry@b815e2e0b42465e6cac11aa7e7717d96863a9120:static/app/components/events/interfaces/spans/waterfallModel.tsx`。新增父子 Span 缩进、相对起点、持续时间条、失败高亮和选中检查器；手机将横向瀑布翻译为纵向调用轨道与相对耗时条。
- `ExpressionDataMapper`：参考 `n8n-io/n8n@9205eb3f1908b94904b6d5400c32dcf3c4baf1b7:packages/frontend/editor-ui/src/features/ndv/parameters/components/ExpressionEditModal.vue`。新增输入数据树、字段引用插入、CodeMirror 表达式编辑和输出预览三方协作；手机使用数据/表达式/输出三个 Tabs。前端不执行任意 JavaScript，复杂表达式交给宿主安全执行。
- `SessionReplayScrubber`：参考 `getsentry/sentry@b815e2e0b42465e6cac11aa7e7717d96863a9120:static/app/components/replays/player/scrubber.tsx`。新增当前回放画面、事件标记、可拖动时间游标、播放/暂停与倍速；拖动只改变当前回放位置，不改写事件轨迹。
- `DashboardGridComposer`：参考 `grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe:public/app/features/dashboard-scene/scene/layout-default/DashboardGridItemEditor.tsx`。新增十二列二维占位、面板选择、移动、收窄/加宽、重置和整版保存；手机将坐标网格翻译为整行/半行触摸布局。所有位置和跨度都经过网格边界收口。
- `LiveLogTailer`：参考 `grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe:public/app/features/explore/useLiveTailControls.ts`。新增日志级别过滤、跟随最新、暂停/继续真实日志流、控制台视窗与选中行上下文；它与静态事件时间线、任务历史的状态模型不同。

六个源码位置均通过 `git cat-file -e HEAD:path`。生成后的六个桌面组件依赖签名和六个手机依赖签名各不相同，`Table` 与 `M.Table` 使用均为零。真实预览保存在 `artifacts/independent-structure-blocks-batch3`，每个类型各有桌面和手机截图。最终 12 张截图的自动门禁结果为：真实数据空态 `0`、卡片横向溢出 `0`、子元素越界 `0`、元信息浮层与按钮/输入框/滑杆/上传区相交 `0`。

## 独立结构族第四批：剖析、合并、鉴权、构建、文档与复制（6 个）

本批目录由 377 增至 383。六个区块均使用唯一 `structureFamily`、明确 `structureDelta`、具名桌面渲染器和独立手机交互；没有使用共享渲染内核连续派生业务别名，也没有将已有表格结构换名计数。

- `FlameGraphProfiler`：参考 `grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe:packages/grafana-flamegraph/src/FlameGraph/FlameGraph.tsx`。新增按调用深度堆叠的比例帧、点击聚焦及自身/总样本检查，不把样本数伪造成耗时；手机将宽幅火焰帧翻译为可切换的火焰层级与调用树。
- `ThreeWayMergeResolver`：参考 `gitlab-org/gitlab@4c7b9936af19984be51a44c779742a0a80a21cb1:app/assets/javascripts/merge_conflicts/components/parallel_conflict_lines.vue`。新增 Base/Ours/Theirs 三方代码对照、逐冲突选择和未解决 Gate，不是只读版本 Diff；手机按冲突折叠，并通过 Tabs 查看三方内容和选择结果。
- `PolicyDecisionSimulator`：参考 `keycloak/keycloak@f03a2104ec6e1fa0b463d71ecb2532a51b228dfe:js/apps/admin-ui/src/clients/authorization/AuthorizationEvaluate.tsx`。新增主体、资源、Scope 评估上下文、逐策略效果链和拒绝优先的最终判定；手机将左右布局翻译为纵向上下文、操作 Selector 和决策轨道。上下文变化后旧结果失效。
- `FormCanvasBuilder`：参考 `nocobase/nocobase@43961f62b3526eda07e080235002ae9ee23c61a7:packages/core/client/src/application/schema-initializer/SchemaInitializer.tsx`。新增字段组件栏、表单画布和属性检查器三方协作，不是运行时表单或步骤向导；手机使用组件/画布/属性 Tabs，默认画布页直接展示已放置字段。
- `PdfPageOrganizer`：参考 `paperless-ngx/paperless-ngx@855669ddf93e25fe5c45420781e191a7df63884d:src-ui/src/app/components/common/pdf-editor/pdf-editor.component.ts`。新增页面缩略图顺序、旋转、页后拆分、删除和完整操作序列，不是附件列表或媒体灯箱；手机使用两列触摸缩略图网格和选中页操作区。
- `StreamReplicationConfigurator`：参考 `airbytehq/airbyte-platform@59be68e17070ac655de596507d73480ed02b61a2:airbyte-webapp/src/area/connection/components/SyncCatalogTable/components/FormControls.tsx`。新增命名空间分组、数据流启停、全量/增量模式、游标与主键联动 Gate，不是普通筛选或字段映射；手机按命名空间折叠，将每条流配置翻译为纵向开关和模式 Selector。

六个固定源码位置均通过 `git cat-file -e HEAD:path`。桌面与手机实现均不依赖 `Table` 或 `M.Table`，行为测试覆盖结构族唯一性、真实渲染和关键状态边界。真实预览保存在 `artifacts/independent-structure-blocks-batch4`，每个类型各有桌面和手机截图，共 12 张；逐张视觉抽查确认结构与交互表达成立，自动门禁结果为：真实数据空态 `0`、卡片横向溢出 `0`、子元素越界 `0`、元信息浮层与交互控件相交 `0`。

## 独立结构族第五批：空间、分析、规则、媒体与建模（6 个）

本批目录由 383 增至 389。候选阶段淘汰了与既有周排期、通用关系图、字段映射、媒体灯箱和步骤向导同构的方案，最终六个类型分别维护空间几何、停靠顺序、字段货架、递归逻辑组、图像变换和数据关联状态，没有共享同一渲染内核换业务名称计数。

- `GeofenceVertexEditor`：参考 `traccar/traccar-web@aada86d5a5bd1cc94ebe5010a9464db695d73a99:src/map/draw/MapGeofenceEdit.js`。新增地图底图、闭合围栏、顶点选择和坐标检查器联动；至少三个不同顶点才能提交。手机保留触摸点位、闭合多边形和纵向顶点操作区。
- `RouteStopSequencer`：参考 `traccar/traccar-web@aada86d5a5bd1cc94ebe5010a9464db695d73a99:src/map/MapRoutePoints.jsx`。新增开放路线、空间停靠点、有序停靠轨道、ETA、上下移动和发布 Gate；阻塞点存在时不能发布。手机用停靠顺序/路线地图/发布三个 Tabs 分离密集操作。
- `PivotShelfComposer`：参考 `apache/superset@3b164e4270860ac07223d4df1a60ca7b56312362:superset-frontend/src/explore/components/controls/DndColumnSelectControl/DndColumnSelect.tsx`。新增字段目录、行/列/值三类货架、字段唯一归属和透视预览，不把一个结果矩阵冒充整个区块。手机使用字段/货架/预览 Tabs，至少有行维度和指标才可保存。
- `BooleanRuleTreeBuilder`：参考 `nocobase/nocobase@43961f62b3526eda07e080235002ae9ee23c61a7:packages/core/client-v2/src/flow/components/ConditionBuilder.tsx`。新增根 AND/OR、组级 AND/OR 和组内条件三层逻辑，空字段、操作符或比较值阻止保存；手机将逻辑组翻译为折叠规则树。
- `ImageCropTransformStudio`：参考 `immich-app/immich@0ff47f417855eb6710e18951c8e705b99698828b:web/src/lib/components/asset-viewer/editor/transform-tool/CropArea.svelte`。新增暗色图像画布、裁剪遮罩、三分网格、比例、旋转、镜像和裁剪尺寸联动；手机保留触摸 Slider 和单列变换操作。提交参数不覆盖源资产。
- `DatasetJoinBuilder`：参考 `metabase/metabase@4dc84c1615d881b69a3a1cfae5bdb46c98a6b8ac:frontend/src/metabase/querying/notebook/components/JoinStep/JoinDraft/JoinDraft.tsx`。新增左右数据集字段面、LEFT/INNER/FULL 策略、字段等式和输出字段集合，不是依赖图、字段映射或结果表。手机将左右字段和输出字段折叠为纵向关联流程。

六个固定源码位置均通过 `git cat-file -e HEAD:path`。六套桌面依赖签名和六套手机依赖签名各自唯一，`Table` 与 `M.Table` 使用均为零。真实预览保存在 `artifacts/independent-structure-blocks-batch5`，六个类型各有桌面和手机截图；截图门禁同时检查真实数据空态、卡片横向溢出、子元素越界和底部元信息浮层遮挡。

## 独立结构族第六批：身份、传输、制造、监控、媒体与调度（6 个）

本批目录由 389 增至 395。六个区块分别维护直接/继承角色集合、HTTP 请求事务、BOM 数量成本递归、阈值区间、媒体入点/出点和 Cron 未来触发状态，没有使用共享列表、步骤或时间轴内核连续派生业务名称。

- `CompositeRoleBuilder`：参考 `keycloak/keycloak@f03a2104ec6e1fa0b463d71ecb2532a51b228dfe:js/apps/admin-ui/src/components/role-mapping/RoleMapping.tsx`。新增可用角色/直接包含双集合、继承角色汇总和自包含防环 Gate；手机用选择角色/有效角色/保存三个 Tabs 区分直接关系与只读继承结果。
- `HttpRequestWorkbench`：参考 `activepieces/activepieces@d05fd537ad3651dc3038c8ce4541635d96c07701:packages/pieces/core/http/src/lib/actions/send-http-request-action.ts`。新增方法与 URL 命令栏、敏感请求头、请求体、发送状态和响应状态/耗时/正文；手机拆成请求和响应两个事务视图。
- `BomAssemblyTreeEditor`：参考 `frappe/erpnext@b55c6d16c90efc862544150d83def414f6d78497:erpnext/manufacturing/doctype/bom/bom_tree.js`。新增装配父子树、逐件数量草稿、单位成本和整树成本递归汇总；手机以折叠根物料、左侧层级线和稳定数量 Stepper 呈现子件。
- `AlertThresholdBandEditor`：参考 `grafana/grafana@eec4a1c66b328851863369c9e031931c9b83cdbe:public/app/features/dimensions/editors/ThresholdsEditor/ThresholdsEditor.tsx`。新增正常/警告/严重连续颜色带、双阈值、当前序列落点和即时状态映射；警告阈值不小于严重阈值时禁止保存。
- `MediaTrimTimeline`：参考 `resource-space/resource-space@8bec4cd30a3d9e218c74b8868ac0783533d38f99:plugins/video_splice/pages/trim.php`。新增入点/出点双柄范围、波形选区、事件标记、片段时长和预览状态；它与单游标回放区块的状态模型不同，提交也不覆盖源媒体。
- `CronOccurrenceBuilder`：参考 `windmill-labs/windmill@5ed846abd2092d9e16195062686ffb13859a2c01:frontend/src/lib/components/CronInput.svelte`。新增五段表达式拆解、预设切换、启停状态、时区和未来触发序列；手机用规则/未来触发 Tabs，五段表达式合法后才允许保存。

六个固定源码位置均通过 `git cat-file -e HEAD:path`。桌面与手机均使用真实专属渲染器，组件依赖不包含 `Table` 或 `M.Table`。真实预览保存在 `artifacts/independent-structure-blocks-batch6`，六个类型各有桌面和手机截图；视觉抽查发现并修复了手机 BOM 名称被 Stepper 挤压的问题，最终截图门禁检查真实数据空态、横向溢出、子元素越界和元信息浮层遮挡。

## 独立结构族第七批：文档识别、查询诊断、数据画像、密钥轮换、事件样例与供应链证明（6 个）

本批目录由 395 增至 401。调研阶段淘汰了两个不成立的候选：Metabase `explorations/query_plan` 实际是分析查询规划协议，不是 SQL 算子执行计划；Immich 重复资产裁决与目录已有 `DuplicateResolutionPanel` 的“每组保留唯一记录”状态模型过近。最终六个类型均有唯一 `structureFamily`、明确 `structureDelta`、具名桌面渲染器和独立手机交互，没有用同一共享内核更换业务名称计数。

- `OcrRegionCorrectionCanvas`：参考 `immich-app/immich@0ff47f417855eb6710e18951c8e705b99698828b:web/src/lib/components/asset-viewer/OcrBoundingBox.svelte`。新增文档底图、OCR 空间边界选择、识别文字校正、置信度告警和逐区域保存；手机用区域/校正双 Tabs，保留触屏区域选择。它不是签名字段放置或图片裁剪画布。
- `QueryExecutionPlanInspector`：参考 `openobserve/openobserve@82d14e8458675a6ee01938b1a80947e2d8f04c45:web/src/components/QueryPlanDialog.vue` 与 `query-plan/QueryPlanTree.vue`。新增逻辑/物理/实际计划模式、父子算子树、估算成本、实际行数、耗时占比与热点检查器；手机将横向 Splitter 翻译为算子树/热点 Tabs。它不是工作流调试图或采样火焰图。
- `ColumnProfileWorkbench`：参考 `datahub-project/datahub@8d9bcaa8ea82ef6d2bd1fbb0bfeb54c7c48dd127:datahub-web-react/src/app/entityV2/shared/tabs/Dataset/Stats/StatsTabV2/columnStats/ColumnStatsV2.tsx`。新增列目录选择、当前列直方分布、空值率、唯一率、取值范围和异常区间联动；手机使用 Selector 切换当前列。它不是若干 Statistic 或普通柱状图的静态拼接。
- `CertificateRotationPlanner`：参考 `goauthentik/authentik@feed2a88462ddf09f04d83d474076b146c2a7f8d:web/src/admin/crypto/CertificateKeyPairListPage.ts`。新增当前/待启用双证书、有效期双轨、重叠窗口 Gate、指纹核对和启用/撤销状态；手机使用纵向轮换阶段与折叠指纹。它不是普通 Timeline、Steps 或证书列表。
- `WebhookPayloadSchemaExplorer`：参考 `activepieces/activepieces@d05fd537ad3651dc3038c8ce4541635d96c07701:packages/web/src/app/builder/test-step/test-sample-data-viewer.tsx`。新增捕获样例状态、字段路径 Schema、类型/必填/样例值联动、原始 JSON 校验和路径采用动作；手机使用 Schema/示例 Tabs。它不是 HTTP 请求发送台或表达式映射器。
- `ArtifactProvenanceVerifier`：参考 `gitlab-org/gitlab@4c7b9936af19984be51a44c779742a0a80a21cb1:lib/supply_chain/artifact_provenance_publisher.rb` 与 `app/models/supply_chain/slsa/provenance_statement.rb`。新增制品摘要、源码身份、构建器、材料和签名组成的来源证明链、可展开证据包、覆盖率和全证据发布 Gate；手机以折叠证据链核对主体、签发方和摘要。它不是部署步骤流或通用证据收集清单。

八个固定源码位置均通过 `git cat-file -e HEAD:path`。目录机械检查结果为 401 个类型、401 个唯一类型、42 个带 `structureFamily` 的区块和 42 个唯一结构族。桌面与手机组件依赖均不包含 `Table` 或 `M.Table`，六套依赖签名各自唯一。真实预览保存于 `artifacts/independent-structure-blocks-batch7`，六个类型各有桌面与手机截图，共 12 张；最终门禁结果为真实数据空态 `0`、卡片横向溢出 `0`、子元素越界 `0`、元信息浮层与交互控件遮挡 `0`。

## 第八批：六个独立状态模型（2026-08-11）

本批继续以结构和状态机计数，不以行业名计数。六个来源固定在已拉取仓库的具体提交：`frappe/erpnext@b55c6d16c90efc862544150d83def414f6d78497`、`DefectDojo/django-DefectDojo@461efedffb081d1a05bdc3a8bc3e1ec760073715`、`openobserve/openobserve@82d14e8458675a6ee01938b1a80947e2d8f04c45`、`shopware/shopware@a0d709c4823afcef3f195cb7d096f9b432bb9217`、`medusajs/medusa@1014c0337027a6087410ad3cc1da9a11fd83ca8d`、`immich-app/immich@0ff47f417855eb6710e18951c8e705b99698828b`。

- `BankTransactionReconciliationMatcher`：参考 ERPNext `MatchAndReconcile.tsx` 与 `SelectedTransactionDetails.tsx`。新增流水轨道、候选凭证匹配分、多凭证选择、已选金额/差额和金额闭合 Gate。桌面为双边勾稽工作台，手机拆成“流水 / 匹配 / 确认”三阶段；不是把两个表并排。
- `CvssVectorCalculator`：参考 DefectDojo `cvss_calculator.js` 与 `cvsscalc31.js`。新增 Base、Temporal、Environmental 指标组、离散度量选择、实时 Vector、分数与严重度，基础指标未完成时不得保存可信分数。手机使用指标组 Tabs 和逐指标折叠，不缩放桌面表单。
- `LogPatternClusterExplorer`：参考 OpenObserve `patterns.rs` 与模式测试数据。新增模板簇、频次、覆盖率、归一化模板和代表样例联动；它与 `LiveLogTailer` 的连续日志流状态不同。手机将模式、样例和统计拆为三个视图。
- `ProductVariantMatrixBuilder`：参考 Shopware `sw-products-variants-generator.js` 与变体生成弹窗。新增属性轴、属性值选择、笛卡尔组合单元、组合启停、SKU/价格草稿和重复 SKU Gate；它不是普通 Table 或 Pivot。
- `InventoryLocationLevelTuner`：参考 Medusa `location-item.tsx` 与 `inventory-stock-form.tsx`。新增地点轨道、stocked/reserved/incoming/available 四段水位、地点启停、草稿变更集，以及存在预留或在途时禁止移除地点的 Gate。手机按地点折叠并使用 Stepper。
- `FaceIdentityAssignmentPanel`：参考 Immich `AssignFaceSidePanel.svelte` 与 `PeopleSearch.svelte`。新增待指派脸部、相似人员候选、已有人员搜索、隐藏人员过滤、创建新人和同身份禁止重指派 Gate；它不是 OCR 坐标编辑或媒体浏览。

六个固定源码位置均通过 `git cat-file -e HEAD:path`。目录机械检查结果为 407 个类型、407 个唯一类型、48 个带 `structureFamily` 的区块和 48 个唯一结构族。桌面与手机渲染器均不使用 `Table` 或 `M.Table`，并由符号图测试要求六套依赖签名各自唯一。真实预览与桌面/手机截图门禁输出到 `artifacts/independent-structure-blocks-batch8`。
